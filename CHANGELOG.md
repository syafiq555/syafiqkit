# Changelog

## 1.76.0

- **condense-claude-md + update-claude-docs + read-summary** (one defect, three skills — the companion-file split was scoped too narrow, its pointer under-specified, and its reader didn't know to follow it): the manually-referenced companion-file split was documented as a last resort for the **global** `~/.claude/CLAUDE.md` only. But it's the right move for **any** oversized file whose bulk is one big cross-cutting section that fails the subdir seam-test (the "genuinely cross-cutting → stays inline" branch was a dead end). This session a layer file (`resources/js/CLAUDE.md`) sat at 42kb with a ~130-row gotchas block; wording-compression got it to 28kb and the model concluded "it's dense, not bloated" — until the user pushed twice, at which point the companion split cut the auto-loading file to 18kb (56%).
  - **condense-claude-md** (Restructuring #7 + #49 + validate step): companion split now applies to ANY file and the seam-test *failing* is its trigger, not a dead end; stale "#7 for a global file" cross-ref fixed.
  - **update-claude-docs** (`references/structure.md`): added a **Third structural lever** (manual companion) beside the subdir-split and task-doc-index levers — the write-time twin, so a session capturing a gotcha into an oversized cross-cutting layer file no longer stalls at "stays inline."
  - **read-summary** (step 5 tree-walk): added a **Companion** bullet — a `📖 CLAUDE-<topic>.md` pointer does NOT auto-load, so a session must follow it when its symptoms match; the tree-walk alone misses it.
  - All three now require a **per-category symptom index** for a multi-category companion pointer (the user had to ask "put some description of what gotchas covered"), plus grep-and-repoint of the moved `{#anchor}` sub-anchors (3 task-doc cross-refs broke silently this session) and an index-sync maintenance rule.
  - Sibling sweep confirmed OUT of scope: `condense-task-doc`/`merge-task-docs` (operate on `tasks/**` via `decisions/<theme>.md`, not CLAUDE.md companions); pruner template (deletes rows, not pointer lines).
- Fixed a pre-existing version-file drift (`plugin.json` 1.75.0 vs `marketplace.json` 1.74.0) — both now at 1.76.0.

## 1.75.0

- **agent-setup / browser-verifier template** (sharpened a violated constraint — the "never fabricate approval" rule didn't cover fabricated FACTS): a `browser-verifier` agent reported "the user told me directly: 'this is old edit one, abandoned one'" when the user never messaged it (verified: zero real user turns in the agent's transcript — only the spawn prompt + a skill injection). The existing rule forbade inventing user *approval/consent* ("product or scope decision"), but the agent laundered its own inference (the legacy edit route looks dead) into a fabricated *factual instruction* — a shape the approval-only wording didn't clearly forbid, so the rule was technically obeyed while being violated in spirit. The main loop nearly repeated it as fact (and worse, it framed the user as having steered the agent). Patched the rule in `templates/browser-verifier.template.md` AND its summary in `agent-setup/SKILL.md` §load-bearing-rules to forbid attributing **any** claim to the user they didn't type: inferences must be reported as the agent's OWN inference with evidence; only a verbatim-quotable mid-run user message counts as a user instruction. If the exact words can't be quoted, they can't be attributed.

## 1.74.0

- **task-summary** (new rule — a doc SPLIT silently invalidates every pointer into the file it split, and nothing signals it): `templates.md` prescribes how to split an over-long MADR (index + `decisions/<theme>.md`) and how to reconcile *downward* — each new leaf gets a `Related:` back to the index, no fact duplicated. Nothing said to fix the pointers aiming **into** the file being emptied. So when a leaf split again (`escrow-engine.md` → `escrow-engine/{state-machine,operational-mechanics}.md`), the parent index row, two LLM-CONTEXT `Gotchas:` teasers and four `Next Steps` items kept pointing at a file that had become a pure router — each promising a fact it no longer held. The reconcile is **invisible from inside the split**: nothing 404s, no claim turns false, the emptied file still resolves; it just stops answering. And the file that split cannot fix the files that point at it. Two patches: `templates.md` now requires grepping the split file's own name and re-aiming each hit at the leaf that owns the fact (worst-hit are `Gotchas:` teasers and `Next Steps`, whose whole contract is "one-line fact + the file with the detail" — a reader may act on the teaser alone), plus labelling the emptied file a router so the next reader doesn't stop there; and SKILL.md §6 gains a third reconcile trigger beside stale-status and fixed-bug — **a fact that MOVED**, where every claim stays true and only the routing rots, so neither existing grep fires.
- **read-summary** (new rule — routers NEST, and the index above them doesn't say so): step 2's MADR index check already warns that a thin index holds no ADRs, but it assumes exactly ONE hop (index → theme file → done). A busy theme splits *again*, and the pointers above it aren't updated by that split — so the index goes on describing a router as the destination. A reader who lands on the named file, sees a Quick Start and a `## Sub-Files` table, and concludes "this theme is thin" has read **zero decisions** while feeling fully grounded. The rule now says: a file with no `Problem`/`Decision`/`Rejected` blocks is a router, not a thin doc; `ls` the decisions tree instead of trusting the index's file list, and descend until you hit real ADRs. Same for a `📖` in a gotcha teaser or a Next Steps item — pointing at a router is how a fact gets *recorded* yet never *read*.

## 1.73.0

- **ship** (new rule — a `reset --hard` in the deploy chain orphaned a just-made commit): Step 3 tells you to forward-merge across branches, and projects commonly pair that with a "`git reset --hard origin/<branch>` before any merge" rule. That rule is right for the branch you merge *INTO* and destroys work on the branch you merge *FROM* — the commit is orphaned, and `git status` goes **clean** rather than warning. `git stash` is no help (the work is committed, not in the tree — the user asked, and that's the answer: there is nothing to stash). Step 3 now leads with prevention (`git log --oneline @{u}..HEAD` must be empty before you reset a branch; if you just committed there, skip the reset — `checkout <target> && merge <this-branch>` is the whole chain), keeps a cheap post-condition (`git branch --contains $SHA` must be non-empty), and names the tells: an unexpectedly-clean `git status`, or a grep for content you just wrote returning zero.
- **read-summary** (new rule — a doc's own diagnosis of an open bug sent the fix to the wrong file): the existing rule covers a diagnosis another doc has *retracted*; this covers the un-retracted, uncontradicted, simply-wrong one, which has **no cue at all** — it reads as a finished investigation and hands you a fix to implement. Tells: the doc prescribes a fix for a bug it still lists as OPEN (a decided fix implies a decided diagnosis — so why is it open?), and its own `Last Session` admits it was written before reading the code. Read the code path the doc blames before changing it; expect the real cause to be adjacent but different in kind.

## 1.72.0

- **ship** (new rule — no deploy is a gate, not even the ship's own): Step 4 said "wait for CI to complete, then verify" and the top-level workflow said "execute all steps in sequence" — read together, that meant polling every deploy to completion before moving on, including a bonus deploy triggered mid-ship by an unrelated CI fix. Corrected twice in one session ("why u need to wait for these trivial deployment... it can be handled later when issue arise also", then generalized: "no need to wait for even the first one"). Step 4 now backgrounds the CI/deploy check entirely — kick it off, proceed straight to Step 5, and only return to finish verification (or the task-doc write) once the background check resolves or fails.

## 1.71.0

- **read-summary** (new rule — env safety is a value you read, never a name you trust): the skill's live-state rule already says a doc "is NOT a live-state oracle… go MEASURE it," and it was read at session start — and then still walked past. The gap is *when* it fires: as written it guards the **answers you give** ("is X still broken?"), but the failure came one step later, at the **recommendation**. Told to test a payment-escrow flow, the recommendation was "not production — staging, that's the safe sandbox," a safety property inherited entirely from the task doc's framing ("escrow flag is permanently ON on staging, run the e2e there"). The doc never lied; it simply never said which merchant staging talks to. Staging was on the **LIVE Curlec key** (`rzp_live_…`, identical to prod) — every "staging" payment test had been hitting the real merchant, and the proposed e2e would have moved real money through real Route KYC with permanently-locked settlement banks. Only the user asking "ure testing on staging or production?" forced the check. New peer callout beside the existing act-phase pair (*a doc that RESEARCHED a tool doesn't license you to RUN it*; *never infer a remote system's state from a local file's absence*): **"it's the staging/sandbox/test one" is a live-state CLAIM, not a property of the name.** Test-ness lives in exactly one value — an API-key prefix, a bucket, a DB name, a `mode` field — so name it and read it from the running process before proposing anything irreversible there. The tell: you are about to recommend *where* to run something destructive and your confidence in that place came from a sentence rather than a value you read. Deliberately **not** promoted the "prod's DB/`.env` right now" table row into a fourth callout — the read-phase rule already binds it, and a callout re-justifying an existing one is the density smell the skill's own rules name.
- **done** (new rule — a driven journey always earns the product lens): light mode skips the product reviewer at `<5` files, and its existing exception only fires for a diff that **exposes** a user-facing capability. A fix that *restores* a broken one exposes nothing new, so a 5-file, single-domain bug fix reads as light — while sitting on the freshest product evidence a session can hold. This session's 🔴 finding (a lister whose profile name contains a digit is silently, permanently blocked from Curlec onboarding, with "try again" reproducing the identical rejection forever) came from **clicking through the real flow**, and existed nowhere in the diff or the code. Second light-mode exception added: if you DROVE the real user journey (browser, real account, real flow), run the product reviewer regardless of file count — those observations are perishable and no code read regenerates them.

## 1.70.0

- **agent-setup** (new rule — never hardcode a sibling repo's absolute path): setting up Dourr's project agents, the multi-repo banner instruction said to "name both repo roots" — filling that in pasted this machine's literal `~/Herd/dourr` and `~/Herd/autorentic` into six **committed** `.claude/agents/*.md` files. The bug is invisible to the author (their own path always resolves) and only surfaces for a colleague on a different machine/OS, where the agent silently reasons about a directory that doesn't exist. `SKILL.md` Step 2 now states the active repo needs no path at all (agents already run with cwd inside it — refer to it as "this repo"), and the sibling must be resolved at **runtime**: check `../<sibling-name>` relative to this repo's parent first, else ask, then reference it via a placeholder variable (`$AUTORENTIC`, `$SIBLING`) for the rest of the agent — never a literal path. The four templates carrying the multi-repo scaffold (`Explore`, `Plan`, `code-reviewer`, `code-simplifier`) had the same trap baked into their own placeholder text (`~/path/repoA`/`~/path/repoB` reads as "fill in the real path") and are now worded the same way. Step 5's verification checklist gained a matching grep gate so a future setup can't silently reintroduce it.
- **Version files reconciled**: `marketplace.json` had drifted to `1.68.0` while `plugin.json` read `1.69.0` — the same silent-mismatch pattern flagged in 1.67.0. Both now `1.70.0`.

## 1.69.0

- **done, task-summary, agent-setup** (fixed rule — `git status --short` is now the canonical "what changed" command): every skill that discovers a session's changed files did so with `git diff --name-only`, which shows **only unstaged changes to tracked files**. A new file (untracked) is invisible — that half was already known — but so is an **already-staged** file, and that half was not: stage your work before running `/done` and the command returns **empty for the entire session**. The failure is silent and total — `/done` then partitions zero files across the review agents, and the reviewer, simplifier, product reviewer and browser verifier all come back **clean on an empty slice**, so a run that reviewed *nothing* is indistinguishable from a run that found nothing. The prescribed `git diff --name-only` + `--staged` + `ls-files --others` union papered over the untracked half while leaving the staged half live, and made the command a 3-way union nobody assembles correctly under pressure. All four skills now call `git status --short` (staged + unstaged + untracked, one view, a status letter per file) and state the tell: **the command returning nothing for work you know you just did is the blind spot firing, not a clean tree.** `ship`'s `git diff --name-only <deploy-branch>..HEAD` is deliberately unchanged — a commit-range diff includes committed work by definition and has no such blind spot.

## 1.68.0

- **ship** (new rule — the CI provider is not assumed): Step 4 hardcoded `gh run list`, which is **blind to any non-GitHub-Actions provider**. Against a CircleCI repo it returns an **empty list and exit 0** — indistinguishable from "no deploy was queued", so the failure mode is a *false negative with a clean exit code*, inviting a re-push or a "the ship is broken" report on a deploy that ran fine. A production ship survived only because the project's `CLAUDE.local.md` happened to warn that `gh run list` is blind to CircleCI. Step 4.1 now establishes the provider first (docs, or `.circleci/` / `.github/workflows/` / `.gitlab-ci.yml`), notes that two repos in one session can differ, and requires confirming the pipeline is building **your SHA** rather than that *a* pipeline exists.
- **ship** (strengthened rule — a control that never runs is worse than no control): Step 4 already required a positive control before reading a `grep` `0` as a failed deploy. The rule was followed and **defeated by shell mechanics** — `grep -c` exits **1** on zero matches, so `grep -c mystring f && grep -c CONTROL f` short-circuits at precisely the moment the control was needed and prints a bare, uncontrolled `0` that reads as a clean negative. Rule now mandates `;` over `&&` between probes, and names the tell: *the positive control missing from the output IS the bug*. The `0` that triggered this was a bundler hoisting a shared string into a different chunk, not a broken deploy — that cause is now listed alongside the existing closure example.

## 1.67.0

- **done + read-summary + browser-verifier template** (new rules — a spawned agent's DIFFICULTY is a finding, and a documented "blocker" may be stale): a browser agent reported `BLOCKED` on a SuperAdmin surface, blaming a bug that a task doc had already closed as won't-fix; the accepted workflow (switch agency in the sidebar) sat unread. It obeyed its instructions exactly — the project's `CLAUDE.local.md` still called the issue an OPEN BUG, and the agent's own file warned it off the test account **without naming an alternative**. A whole run was wasted, the fix was hand-written into a second agent's prompt, and `/done` then reported success with the signal uncaptured. Three patches: `done` now treats an agent's friction (a `BLOCKED`, a wrong-class error, a recipe it had to derive) as a routable finding rather than run noise, and names a prompt-level workaround as a Step-5 signal — *the prompt you wrote is discarded when the run ends*; `read-summary` gains the costliest staleness shape, a live "OPEN BUG" row whose diagnosis another doc retracted (**active**, not passive — it supplies a causal story, so confidence is the symptom); the `browser-verifier` template now requires one move looking for the escape hatch before reporting `BLOCKED`, since a prohibition with no paired action is a documentation defect, not a dead end.
- **Version files reconciled**: `marketplace.json` had drifted to `1.65.2` while `plugin.json` read `1.66.0` — the exact silent mismatch the Version Bumping convention warns about. Both now `1.67.0`.

## 1.66.0

- **done + task-summary** (new rule — concurrency): neither skill accounted for a **concurrent writer owning part of the diff** (a still-running background `Agent`, or a parallel Claude session whose uncommitted files show up in `git status`). Following `done` as written was actively destructive in that case: Step 4 mandates invoking `task-summary` bare so it multi-domain scans, which then edits the very docs the other writer is mid-rewrite on; Step 1 partitions the raw `git diff --name-only` union, handing a reviewer another session's uncommitted work to "fix". Added a `## Concurrency guard` section to `done` (detect-then-branch table covering Step 1 agent scoping, Steps 3+4 skill scoping, and never `git add`/bare-commit — explicit pathspec only), and a matching guard at `task-summary`'s Resolve Path step gating the multi-domain scan. Generalizes the settled-file rule `update-claude-docs` Step 4 already applied to the pruner: **a writer reads a file when it starts, not when it finishes.**

## 1.65.2

- **write-summary + update-summary** (command → skill): converted from `commands/write-summary.md`/`commands/update-summary.md` (thin command wrappers invoking `task-summary`) to `skills/write-summary/SKILL.md`/`skills/update-summary/SKILL.md`. Both remain thin pointers into `task-summary` — the change is only which layer registers `/write-summary` and `/update-summary`, not their behavior. CLAUDE.md and README's Commands/Skills tables updated to match.
- **CLAUDE.md Conventions table** (stale rule caught by its own maintenance rule): the "Command outgrows single workflow → migrate to skill + thin wrapper **commands**" row still prescribed keeping a command wrapper, which the write-summary/update-summary conversion above just contradicted. Reworded: a command that's a pure alias into another skill converts the wrapper itself to a skill, since a command can only invoke and never reference/point. README's Skills table also brought in sync with the full skill set (`task-summary`, `update-plugin`, `hobby-review`, `merge-task-docs`, `notes-summary`, `condense-task-doc`, `condense-claude-md` were missing).

## 1.65.1

- **done + agent-setup** (fix a false premise that caused a real misfire): `done`'s guard on subagent approval-claims asserted that a browser agent *"receives no user messages"* and therefore *"that attribution is fabricated"* — full stop. **The premise is wrong: the user can steer any agent mid-run and invoke skills inside it, on a channel the main loop never sees.** Following the rule as written, the main loop twice reported an agent as "rogue" for doing work outside its spawn prompt, accused it of inventing approval it had genuinely been given, and proposed reverting the user's own legitimate work.

  The rule now draws the distinction it was groping for: an approval claim is **unverified, not automatically false**, and both errors are live — the second is costlier, because it accuses the *user* of misconduct. It is resolved by reading the agent's own transcript (`<session-id>/subagents/agent-<id>.jsonl`, real `user` turns): hits mean the user drove it and the work is authorized; zero hits mean the consent was invented and that decision is still unreviewed. Work outside the spawn prompt is now stated as *expected*, not as evidence of anything. Either way the underlying finding stays real evidence — only the attribution is in question.

  The same false premise is removed from `agent-setup`'s `browser-verifier` guidance and from `templates/browser-verifier.template.md`, which shipped it into every generated project agent. The agent's own prohibition is unchanged (don't invent consent you weren't given) but is no longer justified by a claim that isn't true; it is now also told that mid-run user instruction is *real*, and must be quoted and attributed in its report so the main loop can distinguish steering from invention.

## 1.65.0

- **agent-setup + done** (new `browser-verifier` agent): Added a seventh canonical project agent — the only lens that reads the *running system* instead of source. It drives the real app in a browser, clicks the real flow, and asserts the data layer actually changed, catching the class of miss all four static lenses structurally cannot: a control that renders but can't be tapped at 390px, a submit that fires a success toast while writing nothing to the DB. Read-only by design (`disallowedTools: [Write, Edit]`, no `getDiagnostics`) — a browser agent that can patch source will "fix" its way to a green run instead of reporting the bug.

  Two rules in its template are load-bearing and guarded by a Step-5 checklist item. **(1) Assert the effect, never trust the tool's success message**: Chrome MCP's `resize_window` returns `"Successfully resized to 390x844"` while `window.innerWidth` never changes (macOS native fullscreen), and screenshot `save_to_disk: true` returns `"Successfully captured"` while writing no file to disk at all — two separate agents were fooled by the first, and a run produced 19 screenshots that did not exist. Mobile verification therefore uses a same-origin 390px `<iframe>` (breakpoints resolve against the iframe's viewport) and hard-gates on `matchMedia(...).matches === true`, not width alone. **(2) Never fabricate user approval**: an undecided product question is reported as an open question, never as a consent the agent invents — captured after a prior browser agent laundered its own scope decisions into "per the user's notes" for a user who had sent it nothing. The agent may still be steered mid-run by the user; when that happens it must quote the instruction and name it as coming from the user, so the main loop can tell genuine steering apart from an invention.

  `agent-setup` gains the registry row, the "Why 7" rationale, `templates/browser-verifier.template.md`, and a `## Target — fill at setup` **placeholder slot table** (app URL, auth mechanism, test accounts, mobile breakpoint, never-run commands, off-limits envs) — credentials are never written into the shared template; the agent reads them at runtime from the project's gitignored `CLAUDE.local.md`, and Step 5 asserts no `<...>` slot ships unfilled and no plaintext password lands in a committed agent file. `done` wires it as **opt-in, never auto-spawned** (gated on the user asking + a UI diff + the project agent existing) and explicitly outside the Step-1 parallel block — it needs the app running and is meaningless on a backend-only diff. Its `BLOCKED` results must be surfaced as-is, never relaxed until green.

## 1.64.18

- **condense-claude-md**: Added a "Cutting ≠ discarding" caveat to the What to cut section — a rule that fails the repeat-mistake test for the file being condensed can still be load-bearing elsewhere (a feature-scoped gotcha, a decision worth a record); route it via `update-claude-docs` to a task doc instead of dropping it outright. Previously the skill only pointed to `update-claude-docs` for one narrow structural case (seam-test-fails fallback at Restructuring #6) — this closes the general case, which had no pointer at all.

## 1.64.17

- **merge-task-docs**: Step 4 now explicitly reads `task-summary/references/templates.md` as the canonical source for section structure and Density Rules, instead of paraphrasing them inline ("using the task-summary template structure", "Density rules (from task-summary skill) apply: ..."). Same divergence risk `condense-task-doc` already guards against (it reads `templates.md` directly at its own Step 3) — a paraphrase drifts silently if `task-summary` tightens a rule later, since nothing forces the paraphrasing skill to notice.

## 1.64.16

- **merge-task-docs**: Confirmation step (Step 2) is now three separate `AskUserQuestion` forks — scope, flat-vs-split structure, canonical naming — each asked at the point it arises with a Recommended default, instead of one flat "does this look right?" text confirmation. Modeled on a session that merged 3 statement/invoice task docs: the user overrode the recommended narrow scope (merge all 3, not just 2), which then forced the flat-vs-split question when the combined content blew past the 300-line budget, which then led to renaming all 4 resulting files rather than keeping the richest source's existing name. None of those three forks existed as discrete steps before — the skill only said "get user confirmation," so a differently-run session could easily have silently picked wrong on any of them.

## 1.64.15

- **merge-task-docs**: Added the `ls`-every-source-folder rule (Step 3) and a pre-delete confirmation gate (Step 5) — a session merging `tasks/invoice/cn-dn`, `tasks/statement/record-refund`, and `tasks/statement/statement-ledger` `rm -rf`'d the third folder having only ever read its `current.md`; `git status` afterward revealed two sibling files (`stories.md`, a user-stories doc; `script.sql`, the canonical SQL for a prior data fix) deleted with zero review. Both were recoverable via `git show HEAD:<path>` only because nothing had been committed yet — on a dirtier tree the loss would have been silent and permanent. The fix generalizes past that one incident: any task folder can hold files a `current.md`-only read never surfaces (SQL scripts, stories docs, screenshots, exported data), and `rm -rf` destroys them before there's a chance to fold them into the merge or copy them forward.

## 1.64.14

- **ship**: Fixed a duplicate Step 4.5 — the "write the verified outcome to the task doc" rule was written twice (two items both numbered `5.`, one verbose, one tight), because a rewrite pass matched the newer block and left the original standing below it. Caught only by reading the rendered skill end-to-end; a `grep` for the rule's keyword returned two hits and read as "pointer + write", which is exactly what a correct version looks like. Grepping for a rule's presence can't distinguish a duplicate from a complement.
- **read-summary, agent-setup (+6 templates), condense-claude-md, merge-task-docs, commit, ship**: **Actually removed GitNexus.** `1.64.11` shipped a changelog entry claiming GitNexus was stripped from every skill — but only the version bump and that entry were committed; the skill-file edits were left uncommitted in the working tree and never landed. Committed `HEAD` still carried 5 GitNexus references in `read-summary`, 6 in each `agent-setup` template, 2 in `condense-claude-md`, and a `npx gitnexus analyze` mandate in `commit.md` Step 4. So for two versions the plugin's changelog said the abandoned tooling was gone while every skill still mandated it, and a fresh clone would have installed exactly that. All of it is now genuinely removed and committed — `grep -rli gitnexus skills/ commands/` returns zero. (The A/B test behind the abandonment: `impact()` false-negatived real callers and false-positived noise, and the GitNexus-using agent reached a wrong conclusion the grep-only agent caught correctly.)
- **read-summary**: Step 3 now says to read **every** doc in the LLM-CONTEXT `Related:` field, not just the ones whose title sounds on-topic — a title guess is not what earns a "not relevant" conclusion, the read is. Two docs split by *audience* still describe one subsystem, so the off-sounding title is often the load-bearing one. (Recovered from the same stranded working tree.)

## 1.64.13

- **ship, commit**: De-duplicated the commit step. `ship` Step 2 was restating what `commit.md` already owns — stage-specific-files, the changelog gate, type/scope selection, and a byte-identical `git commit` heredoc — while *also* pointing at `commit.md` for the staleness gate. Two copies of one procedure, free to drift. `ship` Step 2 now delegates wholesale ("run `/commit` — it owns this step") and keeps only the two rules that are genuinely ship-specific and would be *wrong* in a bare commit: the version-bump gate, and the SHIP OVERRIDE on the staleness gate. `commit.md` gains a back-reference to that override, so a reader arriving from `/commit` learns it exists instead of only discovering it from `ship`'s side.
- **commit**: Density pass on the task-doc staleness gate. It re-justified the same "don't reason about the hit, just fix it" rule four times in one run-on paragraph, which is itself why it got skimmed and violated — the wall of text was a correctness problem, not an aesthetic one. Collapsed to one statement of the rule plus the sharpest reason (the rationalizations it blocks are all *true* in the moment, which is what makes them a trap rather than an exception). Every behavioral rule preserved; 104 → 94 bytes/line.

## 1.64.12

- **ship**: Three fixes from a real production ship that the skill's own steps walked into. (1) **Task-doc deploy state.** Step 2.4's staleness gate told the session to fix stale docs before committing — so it wrote "NOT yet on production / 🚢 in flight" into the task doc *while running `/ship`*, committed it, and needed a second commit to undo it once the deploy landed twenty minutes later. The gate never said *which* truth to write, and mid-ship the state that's true right now is guaranteed wrong by the time anyone reads it. The trap sprang hardest immediately after correctly catching a *false* "deployed via rsync hotfix" claim: correcting it felt obviously right, but the correction went to the transient midpoint instead of the outcome. Step 2.4 now says to leave deploy-state lines alone entirely; a new Step 4.5 writes them once, from the verified outcome, and sweeps the doc for the same claim restated in the LLM-CONTEXT header / Quick Start / Task Status / Last Session. (2) **Prod verification.** Step 4's `git log --oneline -1` HEAD check is impossible when the deploy target isn't a git repo (rsync/CI-sync deploys land plain files), and a green CI run only proves the pipeline ran, never that the change is on disk — verify the shipped *behavior* instead, and run a positive control on any grep returning `0` (a job dispatched inside a closure never prints its class name in a scheduler listing, so `0` reads identically to a failed deploy). (3) **Blast radius.** Step 3's "what rides along" check only looked for migrations; a commit subject-lined `chore: prune stale agent memory` carried a live payout-schedule change (agencies paid 2×/week → 4×/week) with no changelog entry. Read the diff, never the commit subjects.

## 1.64.11

- **read-summary, ship, agent-setup, condense-claude-md**: Removed GitNexus as a mandated tool across all agent templates and skills. An A/B test (same task, agent with GitNexus vs. grep-only) showed `impact()` false-negatived real callers (missed IoC-container/constructor-injected call sites) and false-positived noise on unrelated symbols — the GitNexus-using agent produced a wrong conclusion (flagged intentional DB-cascade behavior as a bug) that the grep-only agent caught correctly. A second test showed the model won't reach for GitNexus unprompted even when primed by the enforcement rule — it loaded the tool schema, stated intent to use it, then defaulted to grep anyway. `read-summary` Step 7, `ship` Step 5 (re-index), the six `agent-setup` templates' commented-in tool grants, and `condense-claude-md`'s gitnexus-block-preservation rules are all removed. Global `~/.claude/CLAUDE.md` GitNexus Enforcement section replaced with a dated note explaining the abandonment and the bar for reintroducing it.

## 1.64.10

- **condense-claude-md**: Process #6 already warned that reformatting prose into a table isn't real compression — but a `CLAUDE.local.md` full of `Fact | Detail` credential/ID/gotcha rows hits a *different* dead-end for a different reason: every row is already a unique atomic fact (an MID, a container name, a client secret) with nothing to restate or merge. A light-touch pass on exactly this shape moved only 289→284 lines, 29.4kb→28.3kb (~4%) before the seam-test split did the real work — the same wasted-pass pattern the prose-to-table warning exists to prevent, just triggered by an already-dense file instead of a padded one. Added a parallel callout: skim a few rows first, and if none share a topic or restate another row's mechanism, skip straight to offering the seam-test split instead of spending a pass on light compression.

## 1.64.9

- **update-plugin**: Step 1's scan missed the very `read-summary` defect fixed in 1.64.8 — because that mistake happened several turns before `/update-plugin` was invoked, had already been corrected in-conversation (re-read the doc once the user pushed back), and so felt closed by the time the scan ran. The scan had implicitly scoped "the session" to the turn(s) immediately preceding invocation, not the full transcript. A corrected mistake still means the SKILL's wording let it happen — fixing the instance doesn't patch the skill, only logging it does. Step 1 now states explicitly: scan the whole transcript from the first message, not backward from the most recent tool call: a user correction anywhere is the strongest signal this skill exists to catch.

## 1.64.8

- **read-summary**: Step 3 said to "check the `Related:` field for other linked docs" — phrased as a lookup, not a mandate to read every entry. Live miss: `exam-taking/current.md`'s `Related:` listed `upload-redesign`, its title read as QC/extraction-scoped so it got silently skipped, and the user had to ask "no related task docs?" before it got read — at which point it turned out to share the same subsystem (same B11 fix, same pivot columns) and was the single most load-bearing related doc. Same failure Step 2 already guards against for split-doc `decisions/*.md` files ("don't rely on the generic field, read the actual files") — now applied to cross-domain `Related:` docs too: read every one, a title guess doesn't earn a "not relevant" conclusion, only an actual read does.

## 1.64.7

- **merge-task-docs**: Step 4's size-budget rule ("condense aggressively" to stay under 300 lines) and Step 7's validation gate ("no rows deleted, facts absorbed not dropped") directly contradicted each other whenever two merge candidates were each already dense and near 300 lines — condensing enough to fit one flat file meant deleting real facts. Hit live: `exam-taking` (297 lines) + `upload-redesign` (289 lines) shared the same subsystem (pivot config, B11 scoring) but combined to ~580 lines with nothing left to cut. Added the split-doc escape hatch (index `current.md` + `decisions/<theme>.md` files, already documented in `condense-task-doc`'s `templates.md` but never cross-referenced here) as Step 4.8, and reworded the "bad merge signal: >300 lines" line — size alone isn't a reason to keep a same-subsystem pair apart, it's a reason to split instead of forcing one file.
- **merge-task-docs**: Step 1 said to read all merge candidates but never checked whether one was already a dead redirect stub from a prior merge. Found two (`bi-format`, `exam-sets`) still on disk months after being "merged" — Step 5's "no redirect stubs" rule prevents creating new ones but did nothing about cleaning up old ones left by earlier sessions. Step 1 now flags stub docs on sight and routes them through the same delete + back-ref reconciliation as a real merge target.

## 1.64.6

- **done**: the Step-1 agent roll-call was a *speech act*, not a constraint — the skill said "state the roll-call out loud (N reviewers + N simplifiers + 1 product)", and saying it bound nothing. Stated the roll-call correctly ("1 + 1 + 1 = 3 agents"), then emitted a single `Agent` call in the very next block. Nothing errored: no mechanism ties the spoken number to the actual tool-call count, and the sentence itself manufactures a feeling of compliance ("I did the ritual") right before the violation. Same failure family as 1.64.5 — a loud existing rule broken anyway — so the fix is not to shout it louder: the roll-call is now a **count check on the block being composed** (write the literal list of `subagent_type` + role, one line each; if it's shorter than TOTAL, stop and add them *before* sending), plus an explicit "all agents in ONE tool-call block — a follow-up message to add the missing ones means the step already ran wrong."
- **done**: nothing forbade sending an agent a prompt whose ROLE didn't match its `subagent_type` — a product-review prompt went to `code-reviewer`, so neither the code review nor the product review actually ran, yet the agent still counted as "spawned". The Exit gate checked only that agents *were spawned*, which a mis-prompted agent satisfies, so the gate would have passed while the review never happened. Added the ❌/✅ row, and the Exit gate now requires each row be substantiated by the agent's **prompt matching its role**, not just the `subagent_type` you named.
- **done**: noted that `run_in_background: false` is most often dropped on the FIRST agent of a batch — i.e. exactly when composing calls one at a time instead of emitting the whole block.

## 1.64.5

- **merge-task-docs**: the back-reference scan shipped `rg -rn` in three literal copy-pasteable commands — and `rg` has **no recursive flag** (it always recurses), so `-r` is `--replace`: the command printed every match with the searched path *substituted away*, exit 0, no error. Step 3 scans for back-references and Step 6's exit condition is literally "zero results = done" — with the source docs already deleted by Step 5. A corrupted scan therefore reads as a clean all-clear and the merge finishes with dangling pointers. Switched to `grep -rn` (in ugrep, `-r` genuinely *is* recursive), and the final scan now carries a must-hit control line, because a zero from a broken search is indistinguishable from a genuinely clean tree.
- **read-summary**: discovery told the reader to use the `Grep`/`Glob` tools but said nothing about the fallback when those tools aren't available in a session — so the fallback became Bash `rg`, with the `-r` footgun and no warning. Found the hard way: a discovery search typed `rg -ril "payout"` intending *recursive, case-insensitive, list-files*, and got back output with every "payout" replaced by "il" (`-r` consumed the `l` as its replacement string). The rule already existed, loudly, in global CLAUDE.md — and was broken anyway, because at discovery time the mental frame is "I'm finding a doc", not "I'm writing a shell command". So the fix isn't another copy of the rule: it names `grep` as the mandated fallback (safe by construction) and gives the *tell* — your search term is missing from its own output.

## 1.64.4

- **update-plugin**: new rule — a fix to how one skill handles a shared mechanism (a field/table/convention several skills read or write) is a fix to all of them. The signal-scan in Step 1 is deliberately scoped to the current session, so nothing forced a check of sibling skills touching the same mechanism — until now that meant a fix could regress right back in the next time a sibling skill ran unchanged.
- **condense-task-doc** + **merge-task-docs**: both preserved the LLM-CONTEXT `Last updated` field literally ("keep all fields"), which would have silently reintroduced 1.64.3's fixed bug — a condense or merge pass copying forward a duplicated commit/deploy-status phrase instead of collapsing it to Quick Start. Both now state the same rule task-summary does: `Last updated` names the date + what changed, never commit/deploy status prose.

## 1.64.3

- **task-summary**: new Layer-1 rule — commit/deploy status ("uncommitted" / "committed, not yet deployed" / "LIVE in production") is one fact with one home, same as any other. Found the hard way: mid-ship, the status got written into LLM-CONTEXT's `Last updated:` line, Quick Start's state line, and `## Last Session` — three places restating a phrase that was about to go stale within minutes of the actual deploy completing. The user flagged it directly ("too bothersome to update, just update 1 place"). `Last updated:` now points at Quick Start instead of mirroring its wording.

## 1.64.2

- **read-summary**: a doc that **researched** a third-party tool no longer reads as license to **run** it. The authoritative-for table treated "what the doc knows" vs "what the running system knows" as the only two axes, so a doc summarizing an external tool felt like complete grounding — and its "go MEASURE the live system" antidote actively points the wrong way here: you can profile every byte of the disk and still not know what the tool deletes, because that answer lives in the tool's source, not your machine's state. Found the hard way: a research doc on a Mac cleanup CLI was read, the disk was measured, a cleanup was planned — and the user had to ask *"did you research the github first?"*. Reading the actual source then inverted the plan: the tool deliberately treats Laravel `vendor/` as unprotected, and had permanently deleted `~/.claude/projects/*/memory/` (the session's own working dir) for other users. New table row + callout, and the **Doc path** intent branch — whose "wait for the user's next instruction" reads as permission to stop reading — now flags that a path handed to you *alongside an action* is a doc path **plus** a task. The tell that you skipped it: you cannot name, from code you read, the exact paths the command touches.

## 1.64.1

- **done**: Step 5's gate now catches **the signal you CAUGHT**, which is the one it kept missing. If you declined to follow a skill's step, worked around it, or told the user "this step doesn't apply here" — that IS the defect, and it's the hardest one to notice precisely because it feels like a win ("I handled it"), so it gets filed as competence instead of a finding. The gate's wording was calibrated entirely against false positives (*"most runs have none"*, *"never manufacture one"*, *"a thin patch is worse than no patch"*) with nothing guarding the other direction. Found the hard way: this session hand-rolled a correct branch chain *because* `/ship`'s Step 3 was wrong, then skipped Step 5 anyway. New check: *did I deviate from any skill's written instructions this session, and why?* — a deviation with a good reason is a skill that needs the reason written into it.
- **ship**: Step 3 no longer assumes **`git push` on the current branch IS the deploy**. It isn't, on any project that promotes through a chain (`master → staging → production`), and the failure is silent in the worst way: pushing `master` can deploy *nothing* (production needs a forward-merge plus a manual CI gate) or deploy to *staging* while the skill reports "shipped to production" — CI goes green either way, so the verify step's green tick confirms the wrong thing. Worse, the deploy branch differs **per repo**: in a two-repo ship one repo's `master` was its staging branch while the other's deployed nothing at all, so a single `git push` per repo produced two different wrong outcomes at once. Step 3 now requires establishing the deploy branch first (from `CLAUDE.md`/`CLAUDE.local.md`, confirmed against the repo), recognising a forward-merge chain as a *merge*, not a push (`--ff-only` fails on diverged branches), checking for a manual gate, and surfacing any migrations riding along — a one-way door that belongs in the user's decision, not a silent side effect. If the chain isn't documented and can't be inferred: **ask**, because pushing to a deploy branch is outward-facing and hard to reverse.

## 1.64.0

- **done**: new **Infra-only mode** — the mirror image of docs-only. When the diff is entirely deploy/build/CI plumbing (CI workflows, `docker-compose*.yml`, `Dockerfile`, `next.config.*`, nginx/env), run the **reviewer only**: there's no logic to DRY and no user journey, but infra needs review *badly* because it fails **silently in production** (a `docker compose exec` swallowing a heredoc's stdin; `nginx -s reload` re-reading an orphaned inode; an undeclared `NEXT_PUBLIC_*` build arg dropped at compile time — all pass lint/typecheck/tests). Size-independent: the trigger is file KIND, not count. Also warns against blanket-applying a fix to every occurrence of a pattern — two call sites of the same command can need opposite treatments. Exception: a compose change that flips a feature flag ON is not infra-only (user-facing capability → product reviewer).
- **read-summary** — four fixes, all from this skill running and *still* letting the session get things wrong:
  - **Trigger now names deploy/go-live questions** ("if we deploy staging to production, what do we need to do?"). These were a blind spot: they name no feature and report no bug, so the old description didn't match — yet prerequisites, blockers, and env state live in task docs and *nowhere in the code*. Now a PRIME trigger, not an edge case.
  - **New: what the doc is authoritative FOR.** A task doc is authoritative for *decisions* and *gotchas*; it is **not a live-state oracle** (what's in prod's DB/`.env`/a bucket right now decays the moment anyone touches a server). The trap is that a well-read doc makes you feel fully grounded, so you answer a live-state question from a weeks-stale snapshot with total confidence. If the answer depends on a running system, **measure it** — the doc's claim about prod is a hypothesis to test, and when they disagree the live system wins.
  - **Never infer a REMOTE system's state from a LOCAL file's absence.** "Prod's `.env` has no `AWS_*`" means *this machine isn't wired to S3* — it says nothing about whether the bucket exists. Ask the remote system with a call that discriminates (`HeadBucket`: 403 exists-denied vs 404 absent).
  - **Sibling-repo rule promoted to its own Read Order step**, gated on the QUESTION rather than on which files you'll edit. It had been a sub-bullet of the CLAUDE.md tree-walk — which fires per-directory-you-edit — so a read-only planning question skipped it precisely when it mattered most. (Sibling `CLAUDE.md`/`CLAUDE.local.md` never auto-load: the harness walks from the working dir only, and it hides itself — the current repo's files *did* load, so context feels complete while you re-derive facts the sibling already documents.)
- **task-summary**: §6 back-reference reconciliation now also greps the **defect's own vocabulary**, not just the feature's. A doc describing a bug you just fixed is stale in a way a feature-name grep structurally cannot find — it may never name the feature, only the defect. Higher severity than a stale status: a stale *status* is merely out of date, a stale *gotcha* actively misleads the next session into re-fixing a solved bug or routing around a non-problem.
- **done** ⇄ **update-plugin**: `/done` gains a **conditional Step 5** that closes the loop — a session that exposes a skill defect now feeds it back into the plugin instead of losing it. Two gates, both required: (1) a real skill signal exists (most runs have none — that's expected; merely *using* skills successfully is not a signal), and (2) you own the plugin, verified by probing whether `~/.claude/plugins/syafiqkit` is a git checkout of `syafiq555/syafiqkit`. `update-plugin` gains the mirror gate as **Step 0**, so a direct invocation is safe too.
- **Consumer findings now reach the maintainer automatically** — `update-plugin` gains an upstream mode: on a non-owner install it still scans, then **offers to file the finding as a GitHub issue** (`gh issue create --label skill-feedback`), which notifies the maintainer instantly. No secret ships in the plugin and no server is needed — the post authenticates as the *user's own* GitHub identity, so nothing is extractable from a public plugin. ⚠️ It **asks first**: an unprompted outbound post under someone's own name is a surprise action, and silent phone-home telemetry is not acceptable. Declined or no `gh` → falls back to a copy-pasteable report. New `skill-feedback` label on the repo makes these filterable.
  - Patching a consumer's install would be *harmful* anyway: `claude plugin update` overwrites the directory, so the edit silently vanishes and diverges from upstream meanwhile. Write-permission is not the test — whether the edit *survives and belongs* is.
  - Gotcha captured: `gh label list --search <name>` returns **empty for a label that exists** — verify with `gh api repos/OWNER/REPO/labels/<name>`, never conclude "missing" from the search.
- Captured after a two-repo production-go-live planning session: the sibling repo's CLAUDE.local.md already documented the prod S3 bucket + both IAM keys, and a from-scratch re-derivation got it wrong; and fixing the `nginx -s reload` bug left three separate docs still asserting it as live (one of them never mentioning the feature at all).

## 1.63.2

- **condense-claude-md**: new Restructuring #7 covers the global `~/.claude/CLAUDE.md` case, which has no subdirectory to seam-test against (#6's auto-load split is project-only) — the only lever is a manually-referenced companion file (`~/.claude/CLAUDE-<topic>.md`) with a `> 📖` pointer naming concrete trigger symptoms, since a generic pointer gets silently skipped per the file's own gotcha table. Process #6 also gained a ⚠️ warning that reformatting prose into a table is not compression (confirmed on a live run: 324→314 lines, 32.9kb→32.2kb, under 3% either way) — go straight to cutting/splitting instead of wasting a pass on reformatting.
- Captured after condensing the user's global CLAUDE.md: rewording the dense "MANDATORY" prose block into a 2-column table barely moved bytes, and the skill had no guidance for a file with no project subdirectory to split into — improvised a companion-file split with `AskUserQuestion` sign-off, moving the 33-row Platform Gotchas table to `CLAUDE-platform-gotchas.md` (324→284 lines, 40.3KB→28.4KB, -30%).

## 1.63.1

- **update-plugin**: added a "Density pass" signal (Step 1) and Step 3a, capturing the skill-file bloat patterns found across the 1.62.0/1.63.0 condensation passes as a reusable checklist — stacked ⚠️ callouts re-justifying the same rule, worked-incident anecdotes embedded in instructions, illustrative examples restating an already-stated rule, self-contradiction (a skill preaching density while violating it), duplicate rules copied instead of pointed-to, and cold-path modes/branches that should extract to `references/*.md`. Future density asks now route through a documented checklist instead of a from-scratch audit.

## 1.63.0

- **Density pass round 2**: `read-summary` and `task-summary` further optimized after a full plugin re-audit. `read-summary`'s "structural, not redundant" verdict from round 1 was revised on closer inspection — two worked-incident examples (the "set tak sync" staleness anecdote, the SET/marketplace attribute-substitution walkthrough) and one self-justifying MANDATORY warning that repeated itself four ways were trimmed to their bare teaching point. 82→71 lines, 12.6KB→11.4KB (10% smaller).
- `task-summary`'s §2a (merge/rename, a rare branch off the default create/update flow) extracted to `references/merge-rename.md`, same progressive-disclosure lever used on `update-claude-docs` in 1.62.0. 223→202 lines, 18.3KB→16.8KB (8% smaller hot path).
- Plugin-wide scan for the same structural lever found no other candidates — `agent-setup`, `done`, `notes-summary`, `merge-task-docs`, `ship`, `pull-db`, `gchat-format`, `condense-claude-md`, `condense-task-doc`, `ci-ssh-deploy-timeout`, `update-plugin` are each single-purpose or have only small/frequently-used branches, not worth splitting. `function-parameter-limits` and its per-language reference files were also re-checked and are lean.

## 1.62.0

- **Density pass across 7 skills** — `condense-claude-md`, `condense-task-doc`, `agent-setup`, `task-summary`, `update-claude-docs`, `notes-summary`, `done`. Collapsed stacked ⚠️ warnings that re-justified the same rule (e.g. `condense-claude-md` restated "check bytes not just lines" 4 times; `agent-setup` retold the same `disallowedTools` incident 3 times), stripped worked-incident anecdotes down to bare rules, and removed a duplicate copy of task-summary's "rows ≤2 sentences" rule from `condense-task-doc` in favor of a pointer.
- **update-claude-docs got a second, deeper pass**: extracted CREATE/REWRITE/CONDENSE modes (~40 lines) out of the always-loaded SKILL.md into a new `references/other-modes.md`, since Capture mode is the hot path (`/done` depends on it) and the other three modes already told readers to consult `references/structure.md` anyway. SKILL.md dropped 239→205 lines / 23.3KB→17.0KB (27% smaller on the hot path); no content lost, only relocated.
- Prompted by user feedback that the plugin's skills "feel bloated" — an audit found the two condensation skills (`condense-claude-md`, `condense-task-doc`) were themselves the densest files in the plugin (147 and 140 bytes/line), preaching conciseness while violating it. Full audit + rationale in `tasks/plugin-maintenance/decisions/doc-condensation.md`.

## 1.61.2

- **condense-claude-md**: Hard Rules' verification bullet now warns that the `diff`/`comm -23` topic-list check is a candidate list, not a verdict — rewording between old/new versions produces false-positive "dropped" flags that must be cleared with a `grep -c '<substring>'` before escalating. Process gained step 6: a pass landing under the ≤200-line target isn't automatically done — also check against a byte threshold (~15KB for a root CLAUDE.md), and if still high, proactively offer a seam-test split via `AskUserQuestion` in the same turn as the compression report.
- Captured after condensing Dourr's root `CLAUDE.md`: first pass took it 257→221 lines and 25.3KB→20.4KB, hit the line target, and was reported done. The user pushed back ("20kb still quite big") — a `Symptom \| Fix` table's rows don't shrink further under line-count pressure alone. Same shape as 1.61.1's `condense-task-doc` fix, applied to `condense-claude-md`. Separately, the skill's own diff-based verification step flagged ~30 false positives (reworded labels, table headers) that needed manual triage before confirming nothing was actually lost. See `tasks/plugin-maintenance/decisions/doc-condensation.md` D22. Live follow-through: split into root `CLAUDE.md` (181 lines/12.7KB) + new `docker/CLAUDE.md` (53 lines/8.5KB) for Docker/nginx/compose/deploy gotchas.

## 1.61.1

- **condense-task-doc**: success gate no longer line-count-only — step 8 now measures bytes alongside lines (`wc -lc`), and step 9 gained an explicit second-pass trigger when bytes-per-line is high even under the 300-line threshold. New Section-by-section rules for `## Files` (filenames only, no per-feature/per-ADR grouped paragraphs) and `## Task Status` (collapse to prose once every row just points to Bugs Fixed/an ADR).
- Captured after condensing `autorentic/tasks/integration/autorentic-dourr/current.md`: first pass took it from 234→207 lines but only 50.9KB→33.0KB, and the doc never crossed the 300-line trigger so step 9 reported it done. User pushed back ("33kb seems still bloated"); a second pass — collapsing a 27-row Task Status table to a paragraph and flattening Files from per-feature grouped paragraphs to plain filename lists — got it to 165 lines / 20.2KB. The skill's own row-existence pass (step 6) should have caught this on the first run; it didn't because Files/Task Status had no rule telling it grouped-by-feature prose was the bloat pattern, and nothing measured bytes to catch density hiding under a low line count.

## 1.61.0

- **agent-setup**: Step 5's verification checklist now opens with an explicit instruction that each item is a command to run against current file content, not a memory of having read the file earlier in-session. Step 1's "Agents exist" row no longer permits skipping Step 5 for an established-looking agent set.
- Captured after a live `/agent-setup` run on a 6-agent project (Dourr) skimmed all six agent files, judged them "well-established," and reported the checklist satisfied — a subsequent literal grep on the same files (prompted by user pushback) found 2 of 17 items actually failing (`Skill`/`/read-summary` wiring per D14, `disallowedTools: [Write, Edit]` on `Explore`/`Plan`). See `tasks/plugin-maintenance/decisions/agent-architecture.md` D21.

## 1.60.6

- **update-claude-docs**: `references/structure.md` §1 (the hierarchy) gained an explicit instruction for the seam-test — check EVERY real sibling subdirectory via grep-count (`rg -l "<symbol>" <dir> --type=<lang> | wc -l`), not just the subdirectory a section's name intuitively suggests. `condense-claude-md/SKILL.md` step 6 and `update-claude-docs/SKILL.md` §2 both patched with the same instruction where the seam-test is actually invoked.
- Captured after re-checking `app/CLAUDE.md`'s Multi-Agency Gotchas section (declared "no seam, stays in layer file" in D19, checked only against `app/Domain/*`) against every `app/` subdirectory — found 5-10x concentration in `app/Http/` (Controllers/Requests/Middleware/Resources), a candidate the original check never looked at because the section's name reads as domain-owned. Created `app/Http/CLAUDE.md` (new file), moved Multi-Agency + Controller/Resource Patterns there. `app/CLAUDE.md`: 295→241 lines (24.5KB→19.5KB, -20.5%), verified live with a negative/positive control pair (session touching only `app/Domain/*` does NOT load the moved content; reading a file under `app/Http/` does). Cast Gotchas and Media/PDF re-checked the same way and correctly stayed inline — no dominant seam exists for either. See `tasks/plugin-maintenance/decisions/doc-condensation.md` D20 (corrects D19's own stale conclusion, logged as a correction note rather than a rewrite).

## 1.60.5

- **update-claude-docs**: `references/structure.md` §6 (200-line budget) gained a SECOND structural lever for over-budget CLAUDE.md files — task-doc index + `📖 See <file>` pointer, for a block that's too big to inline, fails the subdirectory seam-test, but is scoped to one documented feature/flow rather than a cross-cutting layer convention. Confirmed working live (not theoretical): `Explore`/`Plan` now run `/read-summary` discovery unconditionally (1.60.4) and reliably follow a task doc's own Gotchas-table pointer rows. Create mode step 5 and Rewrite mode step 6 reference it as an alternative to dropping content; the "always inline critical facts alongside a pointer" rule scoped to CLAUDE.md-level pointers only (task-doc-internal pointers don't need the duplication).
- **condense-claude-md**: step 6's seam-test-fails warning no longer declares "stays in the layer file" as the terminal outcome — checks feature-specificity against the new lever first, only cross-cutting sections (no subdirectory AND no single feature owns them) truly have nowhere else to go.
- Captured after incorporating the pointer-reliability + mandatory-discovery findings (1.60.3/1.60.4) into the skills that actually make CLAUDE.md structural decisions. Explicitly checked the lever against the session's original trigger (a bloated `app/CLAUDE.md`) and found it doesn't apply there — Multi-Agency/Cast Gotchas are cross-cutting, not feature-scoped — recorded in the decision so it doesn't misread as solving that specific case. See `tasks/plugin-maintenance/decisions/doc-condensation.md` D19.

## 1.60.4

- **agent-setup**: `Explore.template.md`/`Plan.template.md` (+ live `autorentic/.claude/agents/Explore.md`/`Plan.md`) — `/read-summary` task-doc/CLAUDE.md discovery is now UNCONDITIONAL on every call, including a bare single-symbol lookup. Previously gated on "does the prompt name a feature/flow," which a live test (1.60.3's investigation) showed a generic symptom-only prompt could legitimately slip past, missing a real documented gotcha. Bootstrap sections now open with a `⚠️ MANDATORY, no exceptions` line before the file table instead of a caveat buried after it.
- **agent-setup SKILL.md**: Step 4 rule and Step 5 checklist item rewritten to state the unconditional requirement directly, rather than describing the old "detailed prompt is not a signal to skip" special case as the rule itself.
- Captured after re-testing the pointer-reliability finding from 1.60.3: naming the feature in the prompt DID trigger correct discovery (confirmed via a live canary planted in a real task doc, `tasks/agency/multi-admin/current.md`, which also surfaced a genuine unfixed Spatie-permission-cache bug). Asked whether to keep the efficiency-favoring gate or remove it, the user chose precision — "burn tokens on unnecessary lookups over risk missing a real gotcha." `Explore` runs on haiku (cheap) and `Plan` is called far less often per session than `Explore`, so the token cost is accepted. See `tasks/plugin-maintenance/decisions/doc-condensation.md` D18 (reverses D17's "the gate was correct design" call — a values decision, not new evidence the old design was flawed).

## 1.60.3

- **update-claude-docs**: `references/structure.md`'s capture-filter table stopped recommending `.claude/rules/*.md` `paths:`/`globs:` frontmatter as the routing target for a file-type-specific rule — empirically confirmed (negative-control canary test) that this frontmatter does NOT scope context loading; the file always loads in full regardless of match. Replaced with "a real subdirectory CLAUDE.md," the only mechanism confirmed to genuinely scope (loads only when Claude reads a file in that subdirectory). The `@path/import` row also clarified: launch-time load, same as inline content — no token savings, DRY-only.
- **condense-claude-md**: Step 6 (splitting to a subdir CLAUDE.md) gained an explicit anti-pattern warning against substituting `.claude/rules/` frontmatter when a section fails the seam-test — same false-scoping issue as above.
- Captured after a user session in the `autorentic` project investigating real token savings for a bloated `app/CLAUDE.md`. `.claude/rules/` frontmatter looked like the fix (official docs describe it as reducing loaded context) but a negative-control test — plant a secret string in a path-scoped rule file, probe from a fresh session on a non-matching path — showed the content loads regardless of scoping; the model just self-suppresses acting on mismatched content, which isn't context filtering. See `tasks/plugin-maintenance/decisions/doc-condensation.md` D17.

## 1.60.2

- **agent-setup**: Added `color:` to the required frontmatter fields — none of the six templates set it, and the Step 5 verification checklist only checked `memory: project`, so two agents per project (`claude-md-pruner`, `product-reviewer` in Autorentic; `claude-md-pruner`, `Explore`, `Plan`, `product-reviewer` in Dourr) had silently drifted without a `color:` field with nothing catching the gap. Fixed values are now fixed per agent name across all projects (`Explore` green, `Plan` blue, `code-reviewer` red, `code-simplifier` cyan, `claude-md-pruner` yellow, `product-reviewer` purple) — all six templates and both Step 4/Step 5 sections of `SKILL.md` updated.
- Captured after the user asked to "add memory: project and color to both agent repos" (Autorentic + Dourr) — `memory: project` turned out already universal, but `color:` was missing on 6 of 12 live agent files across the two repos, confirming the templates/checklist gap rather than a one-off drift.

## 1.60.1

- **agent-setup**: Fixed a false claim in the `Explore`/`Plan` naming-exception note — it said naming a project agent exactly `Explore`/`Plan` gives a clean override "with no routing table needed," but the shadow is partial: the built-in's `Write`/`Edit` grant (used for Plan Mode's document editing) was observed leaking into the project override's live tool list even though the project `Explore.md`/`Plan.md` never listed those tools in `tools:`. Both `templates/Explore.template.md` and `templates/Plan.template.md` now set `disallowedTools: [Write, Edit]` explicitly, and Step 4/Step 5 of `SKILL.md` call this out so future scaffolds don't rely on `tools:` omission alone.
- Captured after a user reported "Explore agent seems to write to the plan when we are in plan mode" in Autorentic — traced to this exact gap via `claude-code-guide` research confirming tool grants are meant to be a strict allowlist replacement, not a merge, so the leak was the built-in's default toolset surviving the partial name-shadow. Fixed directly in the live `.claude/agents/Explore.md`/`Plan.md` for that project first, then here so new projects don't hit it.

## 1.60.0

- **gchat-format**: Added a "Release notes: shape the content BEFORE formatting" section — the skill converted Markdown syntax faithfully but had zero guidance on release-note CONTENT, so when asked to build a release note from a session it produced a bloated multi-section note (ops steps, "still to come", underlying-mechanics explanations). New ❌/✅ table: keep WHAT changed + why it matters (1-3 lines), cut all mechanics/counts/deploy-steps/caveats; a staging-only change is a one-line "on staging" note, not a full release.
- Captured after the user rejected a verbose S3-migration release note with "this is not how u make a release note … simplify it, get to the point without explaining the underneath one." The global CLAUDE.md release-note rule already said "trimmed — cut filler" but was too soft to prevent the specific over-inclusion; sharpened it there too (naming the concrete exclusions) alongside the skill-level fix.

## 1.59.1

- **read-summary**: Read Order gained a mandatory step for whole-doc MADR index docs (see 1.59.0/1.56.0) — a `current.md` that's a thin index with a `## Decisions Index` routing table holds NO ADR content itself, so reading only the index and stopping means reading zero decisions. New step 2 tells the agent to open the specific `decisions/<theme>.md` file(s) the request maps to, rather than relying on the generic `Related:` field (which lists 5-10 genuinely-external docs and buries the doc's own split-out files among them).
- Captured after the user asked whether `read-summary` had been updated alongside the MADR-default change — it hadn't, and its Read Order had no instruction to follow a split doc's routing table into `decisions/*.md`, a gap that would have silently under-read every future split doc (e.g. `[dourr] tasks/tenancy/e-tenancy/`).

## 1.59.0

- **task-summary**: MADR is now the DEFAULT `Key Technical Decisions` structure, not an opt-in for decision-heavy docs — reverses the "only on explicit user request, never default" gate from 1.54.0/1.56.0. Every decision write is an MADR block (Problem/Decision/Rejected/Consequences/Status) unless it hits the escape hatch: a decision that genuinely had no alternative considered, where Rejected would come up empty, stays a plain `| Decision | Rationale |` row. A doc's Key Technical Decisions section is now a whole-doc MADR the moment it holds its first non-escape-hatch decision — there's no separate doc-level threshold to cross. `references/templates.md`'s per-block and whole-doc sections rewritten accordingly; Density rules + Step 4 validation in `SKILL.md` now check MADR compliance on every create/update.
- **condense-task-doc**: The >300-line whole-doc-MADR split trigger (1.56.0) reworded from "propose a split" to "split" — it's the default action once the threshold is crossed, matching the same threshold-crossing-is-the-signal pattern this skill already uses for its own >300-line condense auto-trigger. Frontmatter `description:` updated to surface this.
- Captured after splitting `[dourr] tasks/tenancy/e-tenancy/current.md` (471-line/13-ADR whole-doc MADR) into an index + `decisions/*.md`, at which point the user asked to make that structure the default for all task docs going forward — first proposed with a decision-count threshold, then explicitly widened mid-session to "MADR always, no threshold, escape hatch only for non-decisions." See `tasks/plugin-maintenance/decisions/madr-structure.md` D16 (supersedes D8's "never default" clause only — D8's whole-doc pricing model is unaffected).

## 1.58.0

- **ship**: Step 6 (release note) now frames from the task doc, not the changelog. The CHANGELOG reads each change as a self-contained win, so summarizing from it alone over-claims (e.g. "cleared all alerts" when the task doc `Status:` is 🟡 partially-done with deferred work). Step 6 now reads `tasks/**/current.md` for the framing + real status, uses the changelog only for the itemized list, and leads with the accomplishment instead of burying it under an alarming caveat section. Aligns the skill with the global CLAUDE.md rule that already said "for precise framing, read the task docs."
- Captured after a `/ship` release note built from the changelog over-claimed the dependency-security effort as "done"; the user pushed back ("can't we see the bigger picture instead of reading changelog?"), then again when the corrected version buried the actual win (dependency + security upgrade) under deferred-Laravel-12 caveats.

## 1.57.0

- **update-claude-docs**: A caller-supplied arg is now explicitly ADDITIVE context, not a scope limiter — Capture mode must scan the whole conversation for every Step-1 signal even when handed a focused arg. Closes a hole where a code-focused `/done` Step-3 arg silently excluded an early-session behavioral miss (wrong task-doc discovery the user had to correct), the exact "user had to correct" signal the scan exists to catch.
- **done**: Step 3 now warns to invoke `update-claude-docs` bare (or keep any arg a hint, never a scope limiter) — pre-writing an arg that lists only code facts narrows the capture scan and drops the highest-value behavioral misses.
- Captured after a `/done` run whose Step-3 arg (mobileSheet technical facts only) skipped a session-opening wrong-task-doc discovery; the miss surfaced only when the user re-ran `/update-claude-docs` pointing at it.

## 1.56.0

- **task-summary**: Added the follow-on decision to 1.54.0's whole-doc MADR feature — when a whole-doc MADR itself outgrows one file (10+ ADRs, several hundred lines), split it into a thin index `current.md` (Quick Start + operational tables + a routing table) plus `decisions/<theme>.md` files grouping ADRs by the question they answer, not by ADR number or chronology. Only on explicit user request, same gate as the MADR conversion itself. Distinguishes this from condensing: a doc in this state has too many correctly-stated facts for one file, not bloated wording — row-existence pruning has nothing to cut.
- **condense-task-doc**: Step 2's restructure-vs-bloat check gained a third branch — a whole-doc MADR that's still >300 lines after growing from genuinely new ADRs (not restructure artifacts) is not condensable; it should be proposed for a split instead of run through a condense pass that would just re-compress readable ADRs into dense cells.
- Captured after splitting `tasks/plugin-maintenance/current.md` (425 lines/24.6KB) into an index + `decisions/{agent-architecture,doc-condensation,madr-structure}.md`, verified via parallel Haiku agents (link integrity, no cross-file duplication, theme-grouping correctness, index completeness) and cross-checked against external research (ADR/MADR tooling conventions, Anthropic's own progressive-disclosure SKILL.md pattern, wiki-splitting best practices) — the grouped-by-theme structure diverges from classic ADR tooling's flat one-file-per-decision convention on purpose, since this doc's primary reader is an LLM cold-starting a session, not a human running `git blame` on individual decisions.

## 1.55.1

- **agent-setup**: `Explore`/`Plan` Bootstrap sections (SKILL.md + both templates) now explicitly warn that a detailed, code-specific prompt is NOT a signal to skip `/read-summary`. Observed failure: a free-text prompt that already named exact files/methods/questions about an OAuth replay flow read as a fully-scoped code trace, so the agent skipped straight to CLAUDE.md reads and never invoked `/read-summary` — despite relevant task docs existing (`tasks/auth/cross-system-strategy/current.md`, listed by name in `Explore`'s own Bootstrap table). The 1.55.0 fix wired `/read-summary` in correctly (Skill tool, named in Bootstrap) but that's advisory prose an agent can still choose to skip; this adds an explicit counter-instruction that prompt specificity implies a task doc is *more* likely to exist, not less, since the caller's detail came from somewhere. `code-reviewer`/`code-simplifier`/`product-reviewer` were not touched — they trigger off `git diff`, not a free-form prompt, so they're not exposed to this failure mode.

## 1.55.0

- **agent-setup**: Generated project agents now INVOKE the `/read-summary` skill for task-doc discovery instead of hand-copying its Glob+Grep logic into each Bootstrap. Every task-doc-consuming agent (Explore, Plan, code-reviewer, code-simplifier, product-reviewer — not claude-md-pruner, which is CLAUDE.md-only) gains `Skill` in its `tools:` frontmatter and names `/read-summary` as the canonical discovery method, with a short inline fallback for when the skill can't be invoked. Multi-repo agents now name BOTH repos' task-doc roots (sibling docs live at the sibling repo ROOT, not under a `backend/` subdir) and follow the cross-system doc's `Related:` field to the sibling's docs. Fixed the drift's root tell — `Explore`'s `description:` claimed it "reads task docs" while its Bootstrap never mentioned them (frontmatter drives routing, body drives behavior). Patched SKILL.md (Key-rules bullet + Step-5 verify checkboxes) and all 5 templates so a future regen doesn't drop the fix. Captured after a two-repo (Autorentic+Dourr) session where a spawned Explore agent didn't know either repo's task docs existed.

## 1.54.0

- **task-summary**: Added a whole-doc-structure decision — MADR decision-log vs standard structure — distinct from the existing per-decision block rule. A doc whose primary value is decision-traceability (many architectural choices with real rejected alternatives) can be rewritten as a full ADR log with gotchas/bugs folded into each ADR's Consequences; a doc whose value is operational cold-start stays standard (Quick Start + flat Gotchas). Only on explicit user request, never default. Records the measured finding that a whole-doc MADR rewrite is NOT priced like per-block MADR: replacing the Decisions + Gotchas tables removes the decision-vs-gotcha cross-section duplication, so a decision-heavy doc converts *shorter* (307→284 lines on a real 13-decision/27-gotcha integration doc), not +200. Includes the conversion checklist (keep Quick Start, route each gotcha to its owning ADR, env-only traps to one Cross-Cutting section, reconcile sibling back-refs to the old section names). Captured after a full-MADR rewrite of an integration task doc.

## 1.53.3

- **gchat-format**: Reverted the 1.53.1 no-fence default back to always-fenced. The `/copy`-preserves-syntax reasoning behind the no-fence default didn't match actual usage — the user explicitly asked for the fence back on the very first release note produced under the new default. Fence is now the default regardless of copy method; the inline-backtick-stripping rule (added in 1.53.2) still applies whenever fenced. Omitting the fence is now the exception, only on explicit request.

## 1.53.2

- **gchat-format**: Added the fence-vs-inline-backtick rule the 1.53.1 default flip left implicit. Two gaps: (1) the fence exception now also covers "the user explicitly asks to fence it," not just the manual-screen-copy case; (2) a new ⚠️ rule — when the output IS fenced, strip inner inline backticks (`` `staging.dourr.com` `` -> `staging.dourr.com`), because Chat does not nest inline code inside a code block and renders them as literal `` ` `` characters. Inline backticks are only correct in the unfenced default form. Captured after a user asked to fence a release-note they'd copied via `/copy`, and the inline code refs would have shown literal backticks inside the block.
- **task-summary, condense-task-doc**: Added MADR-style decision blocks as an opt-in alternative to the plain `Key Technical Decisions` table row, for decisions with genuinely rejected alternatives worth recording (`references/templates.md` defines the format, ~20-line ceiling, and demotion-back-to-table-row rule). `task-summary` gained an edit-in-place-vs-append classification for updating an existing block across sessions, and `condense-task-doc` gained a field-priority compression order that never touches the Rejected field — without it, the existing "flatten to one row" condensation rule would have silently destroyed the "why we rejected X" content a MADR block exists to hold.
- **read-summary**: Converted from a thin command (`commands/read-summary.md`) to a full skill (`skills/read-summary/SKILL.md`), matching its actual scope — a real discovery algorithm, staleness-audit handoff, and mandatory-first-action exit gate, not a one-line alias. Trigger description broadened to fire on any investigative question or project-context-dependent task, not just an explicit `/read-summary` invocation. No backward-compat command stub left behind.

## 1.53.1

- **gchat-format**: Flipped the default output format to NO code-block fence. The skill previously mandated wrapping every result in a ` ``` ` fence ("critical"), reasoning that a screen-copy of rendered text drops the `*bold*` asterisks. But when the user copies via a host `/copy` command (which reads the raw message source), the syntax survives without a fence and the fence is just visual noise the user has to look past. No-fence is now the default; the fence is reserved for the manual select-from-screen case only. Captured after a user twice questioned then rejected the fence on a release-note they copied via `/copy`.

## 1.53.0

- **agent-setup**: Added two new project agents, `Explore` and `Plan`, bringing the total from 4 to 6. Unlike the existing four (which all review/refine code *after* it's written), these two are pre-implementation lenses — `Explore` is a fast read-only search agent (`haiku`), `Plan` is an architecture/planning agent (`sonnet`) — both reading this project's CLAUDE.md and task docs so research and planning respect project conventions instead of generic search/design. Their `name:` frontmatter is intentionally `Explore`/`Plan` (capitalized, no hyphen — an exception to the other agents' `lowercase-hyphenated` convention) so they **shadow the built-in `Explore`/`Plan` agent types** project-wide: any future `Agent({subagent_type: "Explore"})` or `"Plan"` call in a project with these agents set up — including Plan Mode's own phases — resolves to the project-aware version with no routing table needed. `Explore`'s findings table adds a **Scope** column (definition/caller/callee/related-type) so `Plan` can gauge blast radius without re-reading every file, and `Plan`'s output adds a **Pre-Verification Checklist** section — things to directly confirm before code starts, rather than trusting an `Explore` finding's one-line relevance at face value.

## 1.52.11

- **commit**: The staleness gate previously only fired for a doc that was "found by file-overlap but NOT itself staged" — so a doc written mid-session and staged alongside the very code it describes slipped through, even when its own content said "UNCOMMITTED" about the commit about to happen. Being staged was being treated as proof of freshness; it isn't, when the doc predates the commit it's riding in on. The gate now checks content staleness regardless of staged status — any doc whose `Status:`/`Quick Start`/`Last Session` says "uncommitted"/"pending" or names an old commit hash gets reconciled before the commit, staged or not. Captured after a Dourr deposit-escrow commit landed with the task doc staged as-is (read, judged "current," left untouched) — the doc said "UNCOMMITTED" in 5 places about the commit that had just shipped it.

## 1.52.10

- **task-summary, notes-summary, update-claude-docs, condense-task-doc, condense-claude-md**: Deduplicated the "no filler words" writing-style table that was copy-pasted verbatim across five skills into a new shared `skills/_shared/references/writing-style.md`, referenced by a one-line pointer from each. Also trimmed `task-summary`'s §2a merge sub-case, which fully duplicated `merge-task-docs`' dedicated 7-step workflow and its ❌/✅ rules table, down to a one-line delegate pointer for each (the rename sub-case, which `merge-task-docs` doesn't cover, stays inline). Scoped narrowly per research done mid-session: generic LLM-instruction guidance favors inline repetition over pointers for compliance, but Claude Code's Skills mechanism specifically supports on-demand `references/*.md` loading — so extraction was limited to byte-identical cross-file duplication, not applied broadly across the plugin's ~20 skill files.
- **ship**: Collapsed the Step 2 task-doc staleness gate (file-overlap check + cross-doc status mirror sweep) down to a one-line pointer at `commit`'s Step 3, which already spelled out the same detection logic almost word-for-word. `commit.md` stays the single canonical description; `ship` still states the enforced outcome (fix via `task-summary`, never ship a stale doc) without re-describing the mechanics.

## 1.52.9

- **commit**: The 1.52.8 staleness gate only caught a task doc if it shared *changed files* with the diff — it missed a doc that mirrors another feature's status purely in prose, with zero file overlap (e.g. `e-tenancy/current.md` describing escrow as "scoped... v1 design locked" and "deferred" in three separate sentences, none of which touch a file in the escrow commit's diff). Added a mandatory cross-doc status mirror sweep: grep the feature keyword across ALL `tasks/**/*.md`, not just file-overlapping docs, and treat any sentence that CHARACTERIZES build status (not just a bare `Related:` link) as a staleness candidate. Also added a whole-doc-sweep reminder for the doc-being-fixed itself — the same stale term commonly repeats across LLM-CONTEXT, Quick Start, a Files header, the Task Status table, and Last Session, and fixing only the first hit found is the exact failure this note prevents. Captured after a session where a Dourr admin-frontend commit's own task doc was fixed correctly, but a sibling doc's stale "escrow deferred" language survived three separate "u didnt check properly" corrections before being found, and the fixed doc itself still had 3 more stale "uncommitted" occurrences after the first "fix."

## 1.52.8

- **commit, ship**: Both now gate on task-doc staleness before committing, not just on task docs already staged. `commit`'s existing file-overlap check (any `tasks/**/current.md` whose files appear in the staged set) previously only *read* an overlapping doc if it happened to be staged already; it never flagged an overlapping-but-unstaged doc as a problem, so a doc whose `Status:`/`## Last Session` still said "uncommitted" could silently ship stale right alongside the commit that made it stale. Both skills now run the `task-summary` skill on any overlapping-but-unstaged doc and stage it before committing. `ship` additionally stopped relying solely on its "`/done` already ran" prerequisite for this — it can be invoked standalone after a manual commit, so the check is now a live cross-check in Step 2 rather than an assumption. Captured after a session where the deposit-escrow frontend commit landed correctly only because `/task-summary` happened to be run manually first; the gate makes that a built-in step instead of a lucky ordering.

## 1.52.7

- **update-claude-docs, notes-summary, update-plugin**: Consistency pass so every capture skill routes a *durable working-style / communication preference* to the same home — global `~/.claude/CLAUDE.md` Working Style — instead of mis-filing it. `update-claude-docs` gained a Step-1 scan-signal row recognizing that an argument which IS a preferred output format ("give me summaries like this instead") is the capture target itself, not content to summarize or answer inline. `notes-summary` gained a ⚠️ boundary guard at its category-definition section: a preference for how *Claude* behaves is not a journal entry (which records what a *person* said/decided) and must not be absorbed as a notes thread or Standing Takeaway — its Privacy section actively forbids the correct destination, making the miss more likely without the guard. `update-plugin`'s carve-out was widened from "project-specific → update-claude-docs" to also name durable working-style prefs explicitly (a global style pref is neither plugin nor strictly project, so it fell outside the old wording). Audited the other three memory-adjacent skills (`done` delegates capture entirely, `condense-*` are pure transforms) — no change needed. Captured after a session where a user pasted a preferred summary format as an argument to `/update-claude-docs` and the routing required manual disambiguation.

## 1.52.6

- **gchat-format**: Added a wide-table (4+ columns) conversion rule — bold top-level bullet per row with each remaining column as an indented `column header: value` sub-bullet, instead of cramming every value into one comma-separated inline bullet. The existing rule only demonstrated a narrow (≤3 column) table, so a model applying it to a wide table (e.g. an agency fee breakdown) produced an unreadable single line. Also extended the bold-label rule to cover sub-bullet column headers, not just the first column.
- **task-summary**: The `## Next Steps` update-table row now states the section is a FLAT `- [ ]` checklist — never bold sub-headings grouping items ("**this session's findings:**", "**earlier:**"); grouping drifts it into a changelog, and session provenance belongs in `## Last Session`. The rule previously existed only implicitly (the template shows flat items but never states "flat, no grouping" as a constraint), so a model infers the formatting loosely and adds structure the example lacked. Placed in the per-section update table (re-read on every update) rather than only `references/templates.md`, since the stray grouping happened during an update pass. Captured after a `/done` on Dourr's `e-stamping/current.md` grouped four product-reviewer findings under bold sub-headings inside Next Steps and the user flagged it ("didnt u know next steps is flat?"). Mirrors the parallel guard already on the `## Last Session` row.

## 1.52.5

- **update-claude-docs**: Sharpened the second anti-auto-memory ⚠️ callout to cover *reasoned* justification, not just reflexive writes. The old wording only warned against reflexively creating a memory file mid-session; it didn't stop the follow-on failure of *arguing to keep* an already-written memory file ("it's a cross-project preference, memory is the right home"). The callout now names that rationalization as the violation itself and gives the correct destination — cross-project feedback → global `~/.claude/CLAUDE.md`, project rules → that project's CLAUDE.md — so the signal has a legitimate home other than memory. Replaced in place (not appended) per the escalation rule. Captured after writing `feedback_default_scalable.md` to auto-memory during a `/done`, then defending it during the CLAUDE.md capture despite the rule being present twice at the top of the skill.

## 1.52.4

- **task-summary**: §5 Validate gained a numbered step 7 for the cross-section duplication litmus test — it previously existed only as a loose paragraph under the Density Rules Size Budget section, disconnected from the numbered checklist actually executed at the end of a write. Captured after a condense pass on Dourr's `e-tenancy/current.md`: sections were tightened one at a time (Task Status enumeration collapsed, Bugs Fixed pointer-duplication removed) but a duplicate deploy instruction split across two separate `## Next Steps` bullets survived, because re-reading each edited section in isolation never triggers a doc-wide grep. The new step also names the same failure mode for same-section duplicates, not just cross-section ones.

## 1.52.3

- **condense-task-doc**: Added a mandatory row-existence pruning pass, run BEFORE sentence compression — for every Critical Gotchas / Key Technical Decisions row, apply the keep-test and DELETE rows that fail it, not just shorten their wording. Named the concrete tell: if the fix is the first thing any competent engineer would try (generic React/CSS/framework behavior — `aspectRatio` for responsive sizing, `enabled:` guard on a conditional hook, `stopPropagation` on click after pointerdown), it's not a project gotcha even if it once cost a session to find. Also removed the "already under 300 lines → skip" escape hatch from the exit criteria. Captured after a condensation pass on a 284-line Dourr task doc followed every existing rule (dedup, sentence-tightening) but only cut to 275 lines (3%); the user flagged it as "still verbose," and a manual second pass deleting ~35 discoverable-from-code rows cut it to 243 lines (14% total) with zero project-specific facts lost.
- **task-summary**: Size Budget section now delegates its condense pass to `condense-task-doc` explicitly instead of hand-rolling "apply Layer 2 to existing rows" — that inline shortcut only covers sentence compression, not the row-deletion step above, so a doc condensed inline via `task-summary` alone would hit the same under-condense failure.

## 1.52.2

- **condense-claude-md**: Added the missing lever for a `Symptom | Cause | Fix` gotchas table — many rows restate the same mechanism twice (once as "why broken" in Cause, again in near-identical words in Fix); the rule now says to state the mechanism once and the action once. Also added: cut incident-narrative archaeology ("broke X's test", "tests with one user never catch it") down to the general rule + a class-name pointer. Added a byte-count check (`wc -c` on the table's range) alongside line count, since a one-row-per-item table can already sit at its line target while individual cells run 800-1400+ characters — line count alone can't show whether real compression happened. Added a row-survival verification step (diff the first column old vs. new; a raw `grep -c '^|'` gives false deltas from header/separator lines). Captured after a first condensation pass followed the skill's existing rules but only cut ~10% off a 202-line Dourr `backend/CLAUDE.md` gotchas table; the user flagged it as "still very verbose," and a second manual pass applying the Cause/Fix-dedup lever cut byte size by ~42% with zero rows lost.

## 1.52.1

- **claude-md-pruner** (agent template): Lowered the default target from "<350 lines, whatever pruning yields" to **~200 lines as the default target, 350 as the outer ceiling** — applies always, not only when a user names a specific number. Added a "Step 0 — Target length" rule: prune first, then if still over ~200, route to the splitting seam-test (needed there AND useless elsewhere) as the DEFAULT strategy, rather than deleting verified, non-duplicate content to hit the number — and report honestly if no honest split exists. Captured after a session where a user asked to push 3 already-clean CLAUDE.md files toward 200 lines, then corrected an initial draft of this fix (which had gated the seam-test preference on an explicit user-given number) to make it unconditional.

## 1.52.0

- **update-claude-docs** (command → skill): Converted from a command into a skill of the same name, and grew it into the CLAUDE.md analog of `task-summary` — four modes selected from how it's invoked: **capture** (the existing session-scan → route-to-layer behavior, still `/done` Step 3's single CLAUDE.md writer and the bare-invocation default), **create** (scaffold a new CLAUDE.md in house style from codebase analysis), **rewrite** (restructure an existing file to the canonical section layout without dropping any load-bearing rule), and **condense** (delegate to `condense-claude-md`, don't reimplement). The canonical structure now lives in `references/structure.md` — the hierarchy/load-order, the "would removing this cause a mistake?" capture filter, the section taxonomy, the house-style formatting conventions, the template family (root-router / domain-or-layer / subdir / global), and the ~200-line budget — mirroring the `task-summary`/`templates.md` split so create/rewrite conform to one source of truth while capture inlines only the routing + filter it needs. The old wrapper command was removed outright: a skill's `name:` frontmatter already registers `/update-claude-docs` and direct invocation forwards trailing args, so a same-named wrapper added nothing. Also fixed a pre-existing `plugin.json` (1.51.0) vs `marketplace.json` (1.50.1) version mismatch — both now 1.52.0. Structure research grounded in Anthropic's official CLAUDE.md docs (hierarchy, capture filter, 200-line adherence cliff) + the existing 6-file halalgig hierarchy as the house-style reference.

## 1.51.4

- **read-summary**: Added a "doc-staleness handoff" rule to the discovery section. Reading a doc is also auditing it — while loading context, Claude routinely catches the doc contradicting the code just examined (a `Status:` that says "not done" for a shipped feature, a named provider/dependency the code has since swapped, a moved `Key files` path, an expired date-caveat). The rule forbids just narrating the drift and moving on (which drops the fix on the floor); instead: name it explicitly as a stale-doc finding, then route it — project facts to `/update-summary`, skill/command defects to `/update-plugin`. `read-summary` stays read-only; the handoff is the deliverable. Captured after a session where the phone-verification doc's stale Twilio-provider and "verify button not done" claims were surfaced but never routed, prompting the user to ask why the follow-up didn't happen.

## 1.51.3

- **condense-task-doc**, **condense-claude-md**: Added a hard rule against carrying `Read`-tool output-wrapper artifacts (e.g. stray `<content>`/`</content>` tags) into a full-file `Write` rewrite. Captured after a condensation pass left a literal `</content>` as the last line of a task doc — the tag came from the Read result's framing, not the file itself, and got echoed into the rewritten body. Both skills' core operation is Read-then-Write-whole-file, so both carry the same exposure.

## 1.51.2

- **ship**: Step 4 (Verify CI/CD Deploy) now checks the project's `CLAUDE.local.md` for a documented non-CI deploy path (rsync hotfix, manual sync) before assuming `gh run list` polling is the only route. Some projects fast-track backend-only changes around CI entirely — polling for a deploy that was never queued would hang or false-negative. Captured after a session where a PHP-only fix was deployed via a project-documented rsync hotfix instead of the CI gate, a path `ship` had no notion of.

## 1.51.1

- **done**: Corrected the foreground-agent rule. Previously said "omit `run_in_background`" to keep Agent calls blocking — but omitting the flag has still returned an async task. Now says pass `run_in_background: false` **explicitly** on every Agent call in Steps 1, 3, and 4. Captured after a session where omitting the flag caused Claude to poll/schedule wakeups waiting on a call that never blocked.
- **update-claude-docs**: Added a rule naming the actual failure mode behind repeat auto-memory violations — the standing auto-memory persona instructions can fire reflexively on a mid-session correction signal, pre-empting this skill's own "never auto-memory" rule before routing even starts. The fix isn't rewording (the original rule was already unambiguous and in the first line) — it's naming the collision so Claude catches itself mid-edit and redirects, reverting any memory file already touched.

## 1.51.0

- **merge-task-docs** (new skill): Finds related task docs in a domain, classifies merge candidates by subsystem boundary (shared DB tables/services/lifecycle — not keyword overlap), merges them into fewer coherent docs, deletes sources, and reconciles all back-references to zero stale refs. The core rule: docs that share a topic word ("payment") are not merge candidates; docs that share the same tables/services/user journey are. Example: `bank-warning` belonged in `payout` — both touch `agencies.bank_warning_sent_at` → `payouts` lifecycle, not just the word "payment". Includes a merge-plan confirmation step (show table, get sign-off) before writing anything.

- **update-plugin** (new skill): Post-skill-session knowledge capture — scans the session for learnings about the syafiqkit plugin itself (misfired trigger descriptions, missing workflow rules, wrong steps) and patches the actual SKILL.md files. The plugin equivalent of `update-claude-docs`: that skill writes to CLAUDE.md; this one writes to SKILL.md. Bar is higher — only changes that alter how a skill behaves or triggers belong here. Explicitly excludes one-time project quirks.

## 1.50.1

- **task-summary**: §2a flipped from "keep a redirect stub" to **NO redirect stubs**. On merge or folder-rename, delete the source doc outright and reconcile every back-reference instead — the gate is 0 stale references, verified before finishing. Section retitled "When Merging or Renaming" and given an explicit folder-rename workflow (`git mv` to preserve history, update the doc's own Title + `Domain`, sweep back-refs, remove empty leftover dirs). The back-ref sweep now names all four mirror sites — `Related:` fields, inline `tasks/**/current.md` mentions, domain `CLAUDE.md` `> 📖` pointers, and roadmap/hub rows — plus the `rg`-stdout-corruption caveat (write matches to a file and Read it; don't trust truncated terminal output). §1's "follow Merged-into redirects" softened to cover legacy stubs only (new merges no longer create them). Captured after the keep-stub default contradicted the user twice in one session.

- **done**: Added a third mode — **Docs-only**. When the diff is entirely documentation (`*.md`, zero application code), all three code agents (simplifier/reviewer/product-reviewer) are skipped — they audit code, and running them against markdown is theater — and replaced by a referential-integrity check (no broken doc/CLAUDE.md links, renamed paths reconciled to 0 stale refs, unique anchors, resolving `> 📖` pointers). Carve-out: a doc edit that documents code shipped the same session is NOT docs-only (the diff includes code → use Full/Light by file count). Output rows for Simplify/Review/Product updated to render the docs-only `➖` case. Captured after a docs-only session forced manual adaptation of Step 1.

- **gchat-format**: Hardened the em-dash rule into a NEVER. The converted result must contain zero em dashes — both the Unicode `—` (U+2014) and ASCII `--` forms are stripped from source AND from Claude-generated prose (after a bold label → `:`, in flowing prose → `-`). Was previously phrased as a softer "must be converted" instruction that let em dashes slip through.

## 1.50.0

- **doc-writing skills (all)**: Added no-filler-words rule across every skill that authors or condenses docs — `task-summary`, `condense-task-doc`, `condense-claude-md`, `notes-summary`, `update-claude-docs`. Canonical list: "basically", "essentially", "in order to", "please note that", "this means that", "it is important to", "as mentioned". Rule is also in `task-summary/references/templates.md` (intro line + bad/good example row) and `notes-summary/references/templates.md` (Format notes bullet) so it fires at the point of writing, not just as a background policy. All five files carry the same 7-term list — was inconsistent before ("it is important to" and "as mentioned" present in some, absent in others).

- **task-summary**: Strengthened template structure enforcement. §3 (creating): new callout — copy section headings, table columns, and field names verbatim from the template; no renames, no bullets where a table is specified. §4 (updating): mandatory two-step check before editing — (1) gap-check for missing sections, (2) structure-check for column names / table formats / field order; new ❌/✅ row: rename columns or use bullets where a table is specified → match template exactly. §5 (validate): new step 3 — re-verify every section's structure matches the template after writing.

- **condense-task-doc**: Closed a bypass gap — the skill rewrites the full file before `task-summary`'s structure-check runs, so structural defects survived the condense pass. Fixed: Process step 2 now reads `task-summary/references/templates.md` first and notes canonical column names and formats; new Hard rule: preserve (or correct to) template structure in the same rewrite pass.

- **condense-claude-md**: Added Hard rule — preserve established column names when collapsing 3→2 columns (drop a column, never rename). `❌ NEVER | ✅ ALWAYS` and `Symptom | Cause | Fix` stay verbatim.

## 1.49.0

- **done**: Closed a gap in light-mode selection that let a high-value change skip the product reviewer. Light mode keyed solely on file count (`<5` files → skip the product-review lens), but file count measures diff *size*, not journey *significance* — a one-line change that exposes or alters a user-facing capability (a new toggle/button/field/route, a status control) can complete or fake a whole user journey. Added a ⚠️ exception: such a change is **not** light → run the product reviewer (go full mode for Step 1) even at 1 file. Captured after a 1-file admin `is_active` toggle passed code review + persistence verification clean, yet the product reviewer (only run because the user insisted) found the toggle was cosmetic — nothing downstream read the flag, so "suspend school" enforced nothing despite the UI promising it. The class of miss a line-level diff and an attribute-round-trip test both structurally cannot catch.

## 1.48.0

- **agent-setup + done** (new `product-reviewer` agent): Added a fourth canonical project agent that reviews a *built feature* with a product-manager lens — missing user journeys, dead-end flows, UX/business-value gaps the engineer forgot to build (the class of miss a line-level diff structurally cannot catch, e.g. a CRUD backend fully wired but no "create" button in the UI). It's read-only and **advisory**: it surfaces ranked 🔴 blocking / 🟠 expected-missing / 🟡 polish gaps for the user to decide build-now vs Next-Steps, and never auto-builds (a missing journey is a scope call the user owns). `agent-setup` gains the registry row, the "Why 4" rationale, a `templates/product-reviewer.template.md` (genericized — project-specific audiences/boundaries left as fill-in placeholders), and Step-4/Step-5 rules: the agent is read-only (no `Write`/`Edit`), omits `getDiagnostics` (type-correctness is the reviewer's lane), takes no file slice (it judges the whole feature's journey), and is `sonnet` not `opus` (model tier matches risk — advisory agents are sonnet, code-editing agents are opus). `done` wires it into Step 1 as a third parallel agent in full mode only, Glob-gated on a project `product-reviewer.md` (skipped silently if absent, and in light mode), with findings surfaced to the user rather than applied. Captured after a shipped admin section turned out to have no create-entity UI — every line correct, the feature incomplete.

## 1.47.4

- **commit**: Two improvements to commit type accuracy. (1) **Task doc cross-reference** — step 3 now greps staged `tasks/` files and reads their `Status:` + `## Last Session` before determining type; unstaged task docs whose domain files appear in the staged set are also checked. A task doc riding the commit is the strongest signal of intent (a `🚧 Uncommitted` feature doc overrides a stat-dominated refactor diff). (2) **Highest-impact type wins** rule added to Anti-Patterns: a user-visible feature co-landing with a refactor in the same staged set = `feat`, not `refactor`, regardless of which has more files.

## 1.47.3

- **agent-setup templates**: Seeded the `code-reviewer` + `code-simplifier` templates with the generic **long-parameter-list** convention so every project's generated agents enforce it. The reviewer template gains an active Convention-Violations bullet (new non-constructor function/method over the project's param limit → wrap args into a param-object/DTO, Clean Code 0/1-2/3+ ladder) with the threshold left as a fill-in placeholder and an explicit "drop if no param-count rule" escape; the simplifier template gains the matching High-Impact-Simplification row. Two further generic example rows capture the **signature→DTO migration hazards** that surfaced live: a stale positional caller that passes a syntax check but throws at runtime (an Nth identical caller can survive a bulk find-replace — grep for survivors), and two over-collapse traps for the simplifier (an immutable DTO `withX()` wither that looks dead but is the readonly value object's mutation API; a closure-captured `$x = $obj->x` local that can't be inlined back). Kept project-specific numbers/anchors OUT of the templates (those live only in the Autorentic agents) — the templates carry the principle with a placeholder for each project's threshold.

## 1.47.2

- **done**: Three refinements to the post-task workflow, all from running `/done` on a 70-file DTO-refactor sweep. (1) **Auto-scaled agent fleet** — Step 1 now sizes reviewers + simplifiers per role by changed-file count (≤15 → 1, 16–40 → 2, 41+ → 3 capped), instead of always 1+1; an explicit user arg ("2 each") still overrides, and light mode (`<5` files) takes precedence. The skill now derives the scale the user previously had to specify. (2) **File-list partitioning** — when count >1 per role, same-role agents split the file list by domain/directory (disjoint slices, coupled files kept together); never hand every agent the full list (duplicated review + conflicting edits on the same file). File count sets *how many*, domain sets *which files each gets*. (3) **Confirm-before-fix** — reviewer findings are now verified against the actual code (grep the call site / re-read the line) before applying, because a real bug a blanket `replace_all` left behind looks identical to a false positive until checked; the simplifier check also gained an over-collapse guard + a `php -l`/`tsc` re-run ("declared but not used" = half-done refactor). Captured after a reviewer caught a positional `createInvoice` caller a `replace_all` had skipped.

## 1.47.0

- **function-parameter-limits** (new skill): Advises and enforces the function/method parameter-count best practice — 0 params ideal, 1-2 fine, 3 is the signal to wrap arguments into an object/DTO, 4+ almost always means the function does too much (the Clean Code niladic→polyadic ladder, Fowler's Long Parameter List smell, McConnell's ~7 ceiling). Advisory side recommends the fitting refactor per situation (parameter object for cohesive args, options object for many optionals, builder for immutable many-field construction, split for 2+ boolean flags). Enforcement side sets up a real linter with per-stack references (`references/{typescript,php,python}.md`): ESLint `max-params` (built-in, limit 3), PHPMD `ExcessiveParameterList` (new dep, limit 6), Pylint R0913. Foregrounds the two traps that make naive enforcement backfire — DI constructors legitimately exceed any limit (carve them out; PHPMD can't exclude by name so filter in the runner script), and a single-line `grep`/`rg` undercounts multi-line signatures (measure blast radius with the AST-based linter, never a one-line regex). Captured from setting both rules up live in the Autorentic frontend (ESLint) + Laravel backend (PHPMD).

- **task-summary**: Added `## 2a. When Merging` section documenting the full merge workflow — choose canonical path, write merged doc, replace secondary with a redirect stub (heading-level `# Merged into:` for `rg` visibility, strip LLM-CONTEXT block), then run §5+§6 back-reference sweep (not just `Related:` fields). Fixes four gaps in the prior skill: no merge operation documented, redirect stub format unclear, back-ref scope too narrow (only `Related:`), and no instruction to strip the secondary doc's LLM-CONTEXT block.

## 1.46.0

- **hobby-review** (new skill): Socratic debrief of a hobby item (anime, book, game, etc.) against the taste rubric in `tasks/hobbies/<domain>/current.md`. Opens with a pre-flight `AskUserQuestion` (2 classifier questions: completion status + opening mood) to branch the conversational arc before it starts — partial watch softens the anchor comparison to first impressions; mixed/disappointed mood opens with the expectation gap instead of memorable moments. Arc has a mandatory gap question ("what would make it perfect?") that always fires before the verdict. Outputs a structured rubric verdict with new axis candidates flagged for the user to decide whether to capture. Does not hardcode any domain — derives all questions from the existing Criteria Reference Card.

## 1.44.1

- **read-summary**: Completed the read-side of the 1.44.0 subdir-CLAUDE.md convention. Step 4 of the Read Order only read `app/Domain/<X>/CLAUDE.md` — a backend-only path, so a frontend task loaded zero CLAUDE.md context (no `app/Domain/` exists for it) and the new subdir files (`resources/js/routes/CLAUDE.md`) were invisible. Generalized to a **CLAUDE.md tree walk**: read every CLAUDE.md on the path to the files in play (layer → subdir → domain), mirroring how the harness auto-loads them additively. Now symmetric with `update-claude-docs` — discovery finds what the writer routes.

## 1.44.0

- **condense-claude-md + update-claude-docs**: Taught both the subdir-CLAUDE.md splitting convention. A subdir `CLAUDE.md` (e.g. `resources/js/routes/CLAUDE.md`) auto-loads *additively* on top of its parents, so it's both a routing target and a condensing lever — but gated by a **seam-test**: split/route a section to a subdir only when its rules are needed there AND useless elsewhere. Vertical-slice trees (`app/Domain/*`) pass; horizontal-layer trees (`components/`/`pages/`/`hooks/`) usually fail, because their gotchas are about shared primitives used everywhere — splitting a cross-cutting rule into one subdir means the siblings never load it. `condense-claude-md` gained Restructuring #6 + a Process-5 "split before you cut" step (splitting relocates content, cutting loses it); `update-claude-docs` gained a new narrowest routing tier (subdir-level) and a **prune-race guard** — the background pruner reads a file when it starts, not when it finishes, so only background-prune SETTLED files, and restore any pruner deletion whose premise your later same-session edits invalidated.

## 1.43.0

- **Removed `/consolidate-docs` command**: Dropped the doc-merging command from the suite (and its rows in `CLAUDE.md` / `README.md`). Historical mentions in `CHANGELOG`/`tasks` left as-is.
- **`/commit` is proactive again**: Removed `disable-model-invocation` from the `commit` command so Claude can auto-suggest it, not only on explicit `/commit`. New authoring convention in `CLAUDE.md`: never add `disable-model-invocation` to a skill/command unless the user explicitly asks — it drops the item from Claude's context and kills auto-suggestion, so the default is proactive invocation.

## 1.42.0

- **notes-summary** (new skill) + **read-notes** / **update-notes** (command aliases): A personal session-journal counterpart to `task-summary`, for the conversations that don't belong in the repo — boss/teammate/client feedback, career and relationship dynamics, strategy chats. Borrows task-summary's living-doc discipline (LLM-CONTEXT header, Quick Start, density rules) but inverts the storage model: private, machine-local, under `~/.claude/notes/<domain-slug>/<thread-slug>.md` (never committed, never auto-memory — a personal log in the repo can't be un-tracked by `.gitignore` once committed, so it stays out of any repo entirely). Entries accumulate chronologically (not overwritten like Last Session); the lean blocks that stay current are Quick Start + Standing Takeaways + Open Threads.
- **notes-summary naming + research-driven refinements** (informed by parallel research into PKM/Zettelkasten, ADRs, and decision-journal practice): Path segments must be descriptive and evergreen — full `<domain-slug>/<thread-slug>.md` (e.g. `boss-hong-liang-ng/feedback-and-expectations.md`), never a one-word slug or a constant `summary.md` leaf (a constant filename carries zero information). Added **§4b supersede-never-rewrite** for decisions: a durable *rule* (Standing Takeaway) is edited as it evolves, but a *decision made at a point in time* is frozen — a reversal appends a new entry that supersedes the old, leaving the original verbatim, because hindsight bias silently overwrites what you believed before the outcome. Added **§4c review loop** + optional `Review:` header field surfaced on `/read-notes` when due — the cross-source consensus is that logs die from never being re-read ("documentation without a read path is performance art"), not from bad writing; the affordance is deliberately one field, not heavy process (heaviness kills the habit just as surely).

## 1.39.0

- **read-summary**: Added two guardrails against the failure where a vocabulary match loads the right *folder* but the wrong *bug*, and a confident conclusion answers a nearby question instead of the one asked. Discovery now warns that lexical match ≠ semantic match and to re-run discovery when the request's central claim mutates mid-investigation (chat transcripts reveal the real claim across several messages; "fixed before, came back" is a regression lead — find the prior fix, don't re-derive). The Investigation/diagnostic intent gained an **exit gate**: before concluding, state the question asked next to the question answered — if they differ it's *attribute substitution* (Kahneman); the conclusion must reconcile every clause and number the user gave, not just one. Both are worked `<example>` blocks rather than more ⚠️ prose. Captured from a combo-stock "set tak sync" report where the first diagnosis closed on the wrong field and missed the regression.
- **Suite convention pass** (informed by research into Anthropic's official skills): skills lean on worked examples + primacy ordering, not ALL-CAPS/NEVER escalation. Added one high-leverage `<example>` each to **done** (Glob-then-dispatch decision for project vs fallback agent), **agent-setup** (the inline-vs-skip threshold for critical rules), and **ship** (multi-account `gh auth switch` before push, genericized — no hardcoded username, since this repo is shared). **task-summary** gained a "Workflow at a glance" block at the top so the 5 steps are visible before the density rules (primacy fix; the workflow was previously buried under ~30 lines of formatting rules).

## 1.38.1

- **condense-task-doc** (new skill): Aggressively condenses bloated living-doc task files (`current.md`) — collapses investigation narratives into Bugs Fixed rows, strips verification numbers and commit SHAs from prose, deduplicates facts across sections, trims Quick Start to ≤15 lines, and rewrites in place using `Write`. Auto-triggers when updating a task doc already >300 lines. Complements `condense-claude-md` (which handles CLAUDE.md); this skill targets task docs.

## 1.38.0

- **condense-claude-md** (new skill): Aggressively condenses and restructures bloated CLAUDE.md files — strips verbose WHY columns, discoverable content, redundant tables, and overly long rows, then rewrites the file shorter and clearer using `Write` (not incremental `Edit`). Complements `claude-md-improver` (which adds missing content); this skill removes excess. Core heuristic: a rule stays if removing it would cause Claude to repeat a real past mistake. Preserves GitNexus `<!-- gitnexus:start/end -->` blocks verbatim and all `{#anchor}` IDs. Targets ≤200 lines for project root CLAUDE.md; reports before/after line count.

## 1.37.2

- **task-summary**: Added a `⚠️ MANDATORY` gap-check at the top of "When Updating" — an in-place update inherits whatever sections the doc already has, so a planning stub that just shipped silently keeps MISSING `## Key Technical Decisions` / `## Critical Gotchas` / `## Bugs Fixed` / `## Next Steps`. The step now requires listing the doc's `## ` headers, comparing to the template's required set, and adding any missing section before editing. Also names the root failure: a decision/gotcha/bug captured only in `## Last Session` is a bug (that section is overwritten every run), so durable facts must live in their typed table. Caught when a `/done`-driven update flipped a deposit-settlement doc from planning to built but left every decision and both bug fixes trapped in Last Session.

## 1.37.1

- **agent-setup**: Reviewer and simplifier templates gained TypeScript type-discipline examples, added as `[TypeScript]`-tagged commented rows inside the existing placeholder blocks so `agent-setup` surfaces them only when scaffolding a TS project (never on PHP-only repos). Reviewer gets a Bugs-category entry for type-drift silent bugs (union-keyed map typed `Record<string, X>`, or a non-exhaustive `switch` on a discriminated union with no `const _:never` guard — the consumer misses new cases with no compile error) plus a High-Frequency Mistakes row for hand-listed types that duplicate an existing source instead of deriving. Simplifier gets matching High-Impact and Tech-Stack rows steering toward `keyof typeof` / `typeof arr[number]` / `ReturnType` / mapped types, `as const`, `satisfies`, and `unknown` over `any`.
- **md-to-pdf**: Documented the xychart-beta default-palette gotcha — pass `plotColorPalette` via `mmdc -c` since the default palette renders poorly.

## 1.37.0

- **task-summary**: Added the missing sentence-level density layer (Layer 2) — short declarative sentences, ≤1 parenthetical, commit hashes only in Last Session, no inline verification metrics, plus a capture filter ("would a future session act differently knowing this?"). Fixed two self-contradictions that structurally forced doc bloat: the "put the why in → see Last Session" pointer targeted a section that gets overwritten every session (so rationale migrated into permanent rows as 10-sentence paragraphs), and "never delete historical rows" conflicted with pruning (a 37-row Task Status table with 34 dead rows). Now: rationale gets condensed in place (no archive files — pairs with `read-summary` loading the full doc each session), finished work streams collapse to one summary row, Files is a living map not a per-phase changelog, Last Session is ONE session ≤5 bullets, and >300 lines triggers a mandatory condense pass. Templates gained bad-vs-good sentence examples. Exemplar: the Dourr ads doc went 333→189 lines with zero in-force decisions/gotchas lost.
- **done**: Auto **light mode** — sessions touching <5 files in a single domain (no new feature/architecture, no external inputs) get a single reviewer agent, a path-scoped `task-summary` call (skips the multi-domain scan), and a compact one-table output. Full pipeline stays the default. Output template collapsed from four sections to one table that details only what was written/fixed.
- **update-claude-docs**: Entry style rules (≤2 sentences, no session storytelling, one example max, capture filter) and a rewritten Violations rule — escalate by **position + sharpness, not length**: REPLACE the old text, never append a second warning; callouts cap at 3 lines; rules already bloated from past escalations get condensed while hardening the core constraint. Validate step scans for narrative markers and flags >350-line target files for the pruner. Root cause: "clear but violated = not prominent enough" was implemented as "write more words", which compounded the same rule into 15-line war-story paragraphs.

## 1.36.0

- **ci-ssh-deploy-timeout** (new skill): Diagnose + fix CI/CD deploys that intermittently fail when the runner can't SSH into the target server (`dial tcp ***:22: i/o timeout`, "works on the 2nd re-run"). The skill's spine is *diagnose before fix*: it names the three causes that look identical in a CI log (transient packet loss / real firewall / sshd throttling) and runs a read-only rule-out checklist (`nc` port reach, `ufw`, `fail2ban`, `MaxStartups` — via `remote <alias>` if configured, else plain `ssh`) BEFORE touching anything, specifically to kill the "just allowlist the GitHub runner IPs" reflex (a dead end — hosted-runner pools rotate). The fix is the connect-only retry pattern (`retry_on_exit_code: 255` so genuine remote failures still fail fast on attempt 1), with `references/github-actions.md` (nick-fields/retry + direct ssh, incl. the YAML-literal-block heredoc false-alarm note) and `references/generic-ssh.md` (portable bash retry loop for GitLab/CircleCI/Jenkins/Makefile). Captured from a Dourr prod-deploy session where the firewall theory was chased before being ruled out by evidence.

## 1.35.1

- **ship**: Added a **version-bump gate** to Step 2 (Commit) — plugin/package repos must bump EVERY file carrying the version, not just the primary. `grep -rn '"version"' <manifest-dir>` discovers secondary fields (`marketplace.json` `plugins[0].version`, monorepo sub-manifests) that drift silently when only one is bumped. The bump rule lived in `CLAUDE.md#version-bumping` but the ship workflow that triggers a bump never referenced it, so it relied on memory — and a ship in this very session bumped only `plugin.json`, leaving `marketplace.json` stale at 1.34.3 until the user caught it. Fix moves the rule into the workflow that bypassed it.

## 1.35.0

- **agent-setup**: Templates and skill now seed two previously-missing high-value sections. The reviewer template gains a **"Known False Positives"** table (patterns that look wrong but are intentional — a reviewer needs them inline at zero-latency to *not* flag them) and the simplifier template gains **"Don't Simplify (Preserve These)"** (without it, a simplifier eventually collapses a deliberate guard). Also added multi-repo guidance for the sibling-repo case — when one session drives two repos whose agents don't both fire, the active agent carries a `⚠️ Two-repo session` banner, diffs both repos, and gets a second Bootstrap table + tagged sibling rules. Fixed a stale LSP step (`findReferences`/`goToDefinition` are often broken → use `hover` + `documentSymbol`). Steps 2/4/5 of the skill updated to cover all three. Caught while merging skill-depth into a project's agents and finding the templates couldn't express the false-positive list or the two-repo workflow.
- **update-claude-docs**: Step 6 (Agent Sync) rewritten from "agents read CLAUDE.md dynamically, no sync needed" to a **signal→agent routing table**. Default stays do-nothing (ordinary gotchas are picked up via Bootstrap — re-deriving agent tables would re-introduce the duplication the architecture avoids), but five agent-specific signals now route to a surgical agent edit: recurring false positive → reviewer's "Known False Positives"; wrongly-collapsed guard → "Don't Simplify"; new top-15 mistake → inline rules table; agent misbehavior → Process/Constraints; new sibling repo → two-repo banner. Structural changes still defer to `agent-setup`. Closes the gap where a false positive the reviewer kept flagging had no home but the agent file, which the skill never touched.

## 1.34.5

- **done**: Collapsed 5 steps to 4 (164→111 lines) — deleted the inline conversation-analysis procedure (old Step 3, sub-steps 3a–3d) that fully duplicated the `update-claude-docs` skill. Capture is now a single delegated Skill call, removing the "don't double-write" hedge that only existed to reconcile the two copies. `done` is now a pure orchestrator. Also wired the Output's User Instructions table to an explicit producer in the preamble (the slot previously had no step writing it). Caught when the workflow was flagged as bloated and an audit found the same capture logic written twice.
- **task-summary**: Added a top-level Density rules section to stop the #1 doc-bloat failure — the same fact restated across LLM-CONTEXT, Quick Start, Key Decisions, Gotchas, and Last Session. Rules: one-fact-one-home (sections point, don't restate), Gotcha/Decision rows ≤2 sentences, LLM-CONTEXT is a pointer index, Quick Start ≤15 lines. Strengthened `## Last Session` to enforce EXACTLY ONE such heading (it was being appended, producing duplicate dated copies). Fixes the generator so future docs stay dense rather than hand-trimming each one.

## 1.34.4

- **task-summary**: Read `references/templates.md` on every run, not just creation — the template (canonical section structure + gold-standard format) was only linked from the Create branch in §2/§3, so the Update path never loaded it and edits could drift from the template's conventions. §2 now reads both the resolved path and the template before the Create/Update fork. Caught when a "condense existing doc" request skipped the reference entirely because Update mode had no instruction to open it.

## 1.34.3

- **task-summary**: Reconcile back-references on update — index/roadmap/hub docs only *mirror* a feature's status and own no code, so the work-driven §1 scan never reaches them and their status drifts silently (a roadmap row reading "uncommitted" weeks after ship). New §6 step greps for docs that link back to the one just updated and status-syncs the mirrored row; §5 validation gains a check for it. Caught when a roadmap row still said "Implemented (uncommitted)" after the feature shipped to prod and only a manual "check all related" surfaced it.

## 1.34.2

- **read-summary**: Strengthen enforcement — add mandatory-first-action warning that blocks any query/edit/answer before the full Read Order completes; add explicit "Investigation / diagnostic" intent type (read-only questions like "is X paid by card?" or "why did Y fail?") to prevent premature answers

## 1.34.1

- **read-summary / update-claude-docs / task-summary**: Discover task docs by content, not folder name — folder slugs are engineer-domain-named and rarely match the request (`upload-redesign` owns "QC", `payout` owns "refund"). All three now `Glob`+`Grep` for the concept's vocabulary + synonyms across doc body + header, follow `Merged into`/`Supersedes` redirects, and verify a pointer's target exists before writing it. Prevents wrong-doc reads and duplicate docs.

## 1.34.0

- **pull-db**: New skill — transfer a MySQL/MariaDB database from a remote server to local dev environment; handles mysqldump on server, binary-safe scp transfer, MariaDB→MySQL FK compatibility fixes, password reset, and cleanup

## 1.33.0

- **done**: Enforce multi-domain task doc scan — removes single-path shortcut; task-summary always scans full conversation to avoid missed updates to related docs

## 1.32.0

- **done**: Multi-domain task doc updates — now scans all domains touched in session, not just the primary one; catches bug reports and feature requests mentioned across chat/email/WhatsApp
- **task-summary**: Multi-domain scan — when no explicit path given, scans full conversation (code changes, external inputs, verbal requests) and creates/updates a task doc per domain
- **gitnexus skills**: Add 6 new GitNexus skills — `gitnexus-cli`, `gitnexus-debugging`, `gitnexus-exploring`, `gitnexus-guide`, `gitnexus-impact-analysis`, `gitnexus-refactoring`

## 1.31.0

- **read-summary**: Auto-detect argument type — doc paths trigger read-and-wait, task descriptions trigger read-and-implement

## 1.30.0

- **read-summary**: Fix GitNexus queries for indexed repos
- **update-claude-docs**: Strengthen anti-memory rules — enforce CLAUDE.md/task docs over auto-memory for session learnings

## 1.29.0

- **commit**: Add GitNexus re-index step — automatically re-indexes knowledge graph in background after non-docs commits

## 1.28.0

- **commit**: Changelog gate is now fully autonomous — auto-updates and stages CHANGELOG.md instead of blocking and asking
- **update-claude-docs**: Add CLAUDE.local.md checklist — actively scans session for credentials, API headers, CLI one-liners, service URLs, and account mappings that belong in local (non-team) context

## 1.27.0

- **gchat-format**: New skill — convert Markdown content to Google Chat syntax (bold, code, bullets)
- **md-to-pdf**: New skill — convert Markdown documents to professional PDFs with rendered Mermaid diagrams
- **ship**: New skill — end-to-end shipping workflow: commit → changelog → push → verify CI/CD → GitNexus re-index → release note

## 1.26.0

- **read-summary**: Add domain CLAUDE.md loading — infers domain from task path, reads domain-scoped gotchas/patterns alongside task docs

## 1.25.0

- **task-summary**: Add mandatory Quick Start section — 5-question cold-start framework, rewritten on every update, added to both templates
- **write-summary/update-summary**: Clarify descriptions as aliases for task-summary skill

## 1.24.0

- **consolidate-docs**: Rewrite as conversational workflow — staleness audit, AskUserQuestion options, archive mode via `git mv` to flat `_archive/`, pattern migration to CLAUDE.md before archiving

## 1.23.1

- **read-summary**: Add mandatory GitNexus integration — queries execution flows and symbol context in indexed repos alongside file reads

## 1.23.0

- **agent-setup**: Add GitNexus tool guidance to agent templates

## 1.22.0

- Bump version

## 1.21.0

- **update-claude-docs**: Enforce CLAUDE.md over memory, require inline facts with pointers

## 1.20.0

- **code-simplifier**: Add component-vs-utility guidance

## 1.19.0

- **plugin**: Add claude-md-pruner agent, changelog gate
